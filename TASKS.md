# Tasks

Living tracker, one entry per module from [docs/21-implementation-roadmap.md](docs/21-implementation-roadmap.md). Update this file as part of any module's implementation, not after the fact.

> **Renumbering note (2026-07-28):** This file follows the new official module order (M1–M12). See DECISIONS.md D-022 for the full old→new mapping if a status entry below looks like it moved.

## Project Setup (pre-module)

- [x] Monorepo scaffolding (Turborepo/pnpm), tooling (ESLint/Prettier/Husky/lint-staged/Docker/GitHub Actions)
- [x] Prisma schema scaffold (superseded by M2's real schema)
- [x] Global exception filter + typed exception hierarchy
- **Status: Complete.**

## M1 — Lead Discovery

- [x] `packages/cache`: Upstash Redis client wrapper (real implementation, replacing the Project Setup placeholder)
- [x] Local dev Redis proxy (`serverless-redis-http`) added to `docker-compose.yml` — see DECISIONS.md D-006
- [x] `packages/shared-types`: `DiscoveryJob`/`Lead` zod schemas + types
- [x] `places.adapter.ts` — three-tier Places API (New) fetch strategy
- [x] `website-fetch.adapter.ts` — SSRF-guarded site fetch (22 dedicated tests)
- [x] `website-status.classifier.ts` — heuristic classifier, AI-fallback port deferred (DECISIONS.md D-005)
- [x] `cost.service.ts` — quota enforcement + `cost_event` logging
- [x] `discovery` module: `POST/GET /discovery-jobs`, `GET /discovery-jobs/:id`, pipeline runner
- [x] `leads` module: `GET /leads`, `GET /leads/:id` (read-only scope; mutations are Module M4)
- [x] Unit tests for every new service/adapter/classifier (143 unit tests total in `apps/api`)
- [x] Integration tests (NestJS TestingModule + Supertest) — mocked data layer, see DECISIONS.md D-008 (15 e2e tests total)
- [x] Verified: compiled server (`nest build` → `node dist/main.js`) boots, all discovery/leads routes map, auth guard protects them
- [x] Verified: `*.spec.ts` excluded from the production build output (`tsconfig.build.json` added — was silently missing since initial project setup)
- [x] **Full engineering audit performed and closed** (architecture, SOLID, performance, security, error handling, logging, validation, scalability, test coverage) — 8 real issues found and fixed, see DECISIONS.md D-010 through D-016:
  - Cost-quota check-then-act race → atomic `CostService.charge()` (reserve-first)
  - Non-transactional multi-category job creation → `prisma.$transaction([...])`
  - Job could strand in `QUEUED` forever on a DB-update failure → RUNNING update moved inside the try/catch
  - No general rate limiting → `@nestjs/throttler`, global 100/min + 10/min on `POST /discovery-jobs`
  - Whitespace-only `city`/`categories` passed validation → `.trim()` added
  - Unbounded 1-character `q` search → `.min(2)` added
  - No index on `businessName`/`city`/`category` → added (trigram/GIN follow-up documented, deferred — needs a live DB to verify)
  - `:id` params not validated as UUIDs → `ParseUUIDPipe` added to both `GET /leads/:id` and `GET /discovery-jobs/:id`
  - 20 new/updated tests added for the fixes above (unit + 2 new e2e suites); 146 unit + 20 e2e = 166 tests total, all passing
- [ ] **Not done in this environment:** real Trigger.dev wiring (D-004), live Postgres/Redis verification (D-008), a real Google Places API key smoke test — all explicitly flagged, not silently skipped
- **Status: Implementation + audit complete, pending your approval.**

## M2 — Database & Core Domain Models

Now the permanent, official identity of "M2" (DECISIONS.md D-022) — resolved from an earlier discrepancy against Doc 21's original "Lead Pipeline / CRM" scoping. That original mutation-endpoint scope now lives at **M4 — Lead Management APIs**, unbuilt.

- [x] Reconciled the brief's requested entity list (`Business, BusinessCategory, Lead, LeadStatus, Project, Website, User, Role, AuditLog`) onto the existing, already-approved schema via `AskUserQuestion` — `TeamMember`/`TeamRole` = User/Role, `Website` = Project, enums stay enums, current NestJS-idiomatic services = "the repository layer" (no new Repository interface/impl split) — DECISIONS.md D-017
- [x] `Business` split out of `Lead` — new model owns all business-data fields; `Lead` slims to pure pipeline state + unique `businessId` FK; `BusinessAnalysis`/`DiscoveryJob` retargeted accordingly; `Website`/`SalesProposal` untouched — DECISIONS.md D-018
- [x] Soft-delete Prisma Client Extension (`packages/db/src/soft-delete.extension.ts`) — structural `deletedAt: null` scoping + delete→update rerouting for `TeamMember`/`Business`/`Lead`/`Website`, replacing M1's manual per-query filtering — DECISIONS.md D-019, 26 unit tests
- [x] Initial migration generated without a live DB (`prisma migrate diff --from-empty`) — `packages/db/prisma/migrations/20260728000000_init/` — DECISIONS.md D-020
- [x] `packages/db/prisma/seed.ts` extended with `Business`/`Lead` fixtures (one per `WebsiteStatusType`, only `none`/`outdated` get a `Lead`)
- [x] New `apps/api/src/business/` module — `BusinessService` (`findByPlaceId`, `findById`, `upsertByPlaceId`), `BusinessModule`, `business.mapper.ts` (website-status mapping, moved off `lead.mapper.ts`)
- [x] `LeadsService` reworked — `findMany`/`findById` join `Business` via Prisma `include`, `upsertByPlaceId`/`existsByPlaceId` replaced with `ensureForBusiness` (create-if-none-exists, idempotent on `businessId`)
- [x] `DiscoveryRunnerService` reworked — calls `BusinessService.upsertByPlaceId` then `LeadsService.ensureForBusiness`; "already discovered" gate for a `present` result now checks Business existence, proven equivalent to the old Lead-existence check (DECISIONS.md D-018)
- [x] `lead-response.dto.ts` composes the (unchanged) flat API response from the `Lead & { business: Business }` join
- [x] `packages/db` gained its own vitest suite (matching `packages/cache`/`packages/shared-types` convention) — DECISIONS.md D-021
- [x] All affected tests updated/added: `leads.service.spec.ts` rewritten, new `business.service.spec.ts`, `discovery-runner.service.spec.ts` rewritten for the Business-first flow, `discovery.e2e-spec.ts`/`rate-limit.e2e-spec.ts` fixtures updated to the joined shape
- [x] Full validation: `packages/db`/`apps/api` (and every other touched package) typecheck, lint, unit tests, e2e tests, and `apps/api` build all pass; compiled server boots and maps every route including the new `BusinessModule` wiring
- [ ] **Not done in this environment:** running the generated migration against a real Postgres (D-020), `prisma migrate diff --from-migrations` drift check (needs a shadow DB) — both explicitly flagged, not silently skipped
- **Status: Implementation complete, pending your approval.**

## M3 — Authentication & RBAC

Authentication (Clerk integration, domain-restricted login, `ClerkAuthGuard`, `GET /me`, `POST /webhooks/clerk`) was retroactively assembled from the original "Platform Foundation" work (DECISIONS.md D-022) and already complete. This round adds the authorization layer the founder's M3 brief actually asked for — RBAC, permission middleware, role hierarchy, organization future-readiness, and audit logging — after resolving a real architecture conflict (the brief's original ask was a self-rolled JWT/password/refresh-token system, which would have replaced Clerk; the founder confirmed Clerk stays and M3 is authorization-only, DECISIONS.md D-023).

- [x] Six-role taxonomy — `TeamRole` enum replaced (`SUPER_ADMIN, ADMIN, SALES_MANAGER, DEVELOPER, SALES_EXECUTIVE, VIEWER`), hand-authored incremental migration (`packages/db/prisma/migrations/20260729000000_m3_rbac_role_taxonomy/`), `packages/shared-types`/mapper/seed data updated — DECISIONS.md D-024
- [x] Role hierarchy — `apps/api/src/common/rbac/role-hierarchy.constants.ts`, `@MinRole()`/`MinRoleGuard` for "at least this seniority" checks — DECISIONS.md D-025
- [x] Permission middleware — `apps/api/src/common/rbac/permission.constants.ts` (8 permissions, explicit per-role matrix grounded in Doc 15's existing AuthZ statement), `@RequirePermissions()`/`PermissionsGuard` — DECISIONS.md D-026
- [x] Applied to existing routes: `LeadsController` (`leads:read`), `DiscoveryController` (`discovery:run`/`discovery:read`) — additive only, no existing role loses access it had
- [x] Organization support (future-ready) — commented-out `Organization` model + `TeamMember` doc-comment, following the project's existing future-model convention — DECISIONS.md D-027
- [x] Audit logging — `AuditLogService` (real write path for the previously-unused `AuditLog` table), `@Audited()`/`AuditLogInterceptor` (global, opt-in), best-effort (a log failure never fails the action) — DECISIONS.md D-028
- [x] `RbacModule`/`AuditModule` registered globally in `app.module.ts`, guard chain: `ClerkAuthGuard → RolesGuard → MinRoleGuard → PermissionsGuard`, interceptor: `AuditLogInterceptor`
- [x] Unit tests: `role-hierarchy.constants.spec.ts`, `permission.constants.spec.ts`, `min-role.guard.spec.ts`, `permissions.guard.spec.ts`, `audit-log.service.spec.ts`, `audit-log.interceptor.spec.ts` (55 new tests) + every existing role-referencing spec updated for the 6-role taxonomy
- [x] Integration test: `apps/api/test/rbac.e2e-spec.ts` — dedicated throwaway controller (not real API surface) proving the full guard+interceptor chain over real HTTP requests, 22 tests
- [x] Full validation: 198 unit + 42 e2e tests in `apps/api`, full monorepo typecheck/lint/build all pass, compiled server boots with `RbacModule`/`AuditModule` wired and every route still mapping
- [ ] **Not done in this environment:** running the role-taxonomy migration against a real Postgres — same D-020/D-024 constraint, explicitly flagged
- **Status: Implementation complete, pending your approval.**

## M4 — Lead Management APIs

Builds on Module M2's `Business`/`Lead` split and Module M3's RBAC — the mutation-endpoint scope originally described as "Lead Pipeline / CRM" (see M2's note above). Architecture review before implementation surfaced four schema-shape conflicts against the frozen M2 model (standalone lead creation, notes-as-a-single-field, no tag support, activity vs. audit-log overlap); resolved via `AskUserQuestion` before writing code, not guessed at — DECISIONS.md D-030 through D-032.

- [x] Schema: `Lead.tags` (`String[]`, GIN index), `LeadNote` (append-only, authored), `LeadActivity` + `LeadActivityType` enum (rep-facing timeline, distinct from `audit_log`) — DECISIONS.md D-030, D-031, D-032
- [x] Hand-authored migration (`packages/db/prisma/migrations/20260730000000_m4_lead_management/`) — backfills existing `leads.notes` content into `lead_notes` before dropping the column, same no-live-DB constraint as D-020/D-024
- [x] `packages/shared-types`: `createLeadSchema`, `updateLeadSchema` (strict, empty-body-rejecting, `null`-vs-omitted-aware), `createLeadNoteSchema`, `leadNoteSchema`, `leadActivitySchema`, `LEAD_SORT_FIELDS`, tag validation (lowercased, length- and count-capped) — 41 unit tests
- [x] Typed exceptions: `LeadNotFoundException`, `BusinessNotFoundException`, `DuplicateLeadException`, `TeamMemberNotFoundException`, `InvalidSortFieldException`
- [x] `LeadActivityService` (transaction-aware recorder + paginated reader) and `LeadNotesService` (append-only notes, author-attributed) — both structurally logged
- [x] `LeadsService`: `create` (business-must-exist, one-lead-per-business, optional assignee validated), `update` (PATCH semantics, per-field activity diffing, multi-field PATCH produces multiple timeline entries), `softDelete` (reroutes through the Module M2 soft-delete extension), sort/filter/tag-filter on `findMany` — `ensureForBusiness` (discovery's write path) unchanged in contract
- [x] `LeadsController`: `POST/PATCH/DELETE /leads(/:id)`, `POST/GET /leads/:id/notes`, `GET /leads/:id/activity` — per-route `@RequirePermissions()` (`leads:read`/`leads:write`/`leads:delete`), `@Audited()` on every write route
- [x] Structured logging (`Logger`, matching the existing `DiscoveryRunnerService` convention) on every mutation in `LeadsService`/`LeadNotesService`
- [x] Unit tests: 60 new/updated tests across `leads.service.spec.ts`, `leads.controller.spec.ts`, `lead-activity.service.spec.ts`, `lead-activity.mapper.spec.ts`, `lead-notes.service.spec.ts` (241 total in `apps/api`, up from 198)
- [x] Integration tests: `apps/api/test/leads.e2e-spec.ts` — 29 tests covering CRUD, workflow, the RBAC permission matrix, validation, and audit-on-write (71 e2e total, up from 42)
- [x] Full validation: monorepo typecheck/lint/build clean, compiled server boots with all 8 `/leads*` routes mapped
- [ ] **Not done in this environment:** running the migration (incl. the notes backfill) against a real Postgres — same D-020/D-024/D-029 constraint, explicitly flagged
- [ ] **Deliberately out of scope, per instruction:** pipeline list/kanban UI and the global `/search` endpoint+UI that Doc 21's original M4 entry also listed — this pass is the backend API only; M4 isn't fully "done" against that original DoD until a UI exists, flagged rather than silently narrowed
- **Status: Implementation complete, pending your approval.**

## M5 — Google Places Synchronization

Architecture review before implementation found the requested feature set didn't fit the frozen M1/M2 schema/design without either a schema change or an interpretation call in three places (Business schema shape, the M1 refactor's scope, and the provider-abstraction naming). Resolved via `AskUserQuestion`/direct confirmation with the founder before writing code — DECISIONS.md D-033 through D-036.

- [x] `LocationProvider` interface (`apps/api/src/common/providers/`) — `search()`/`getWebsiteUri()`/`getDetails()`, one page per `search()` call; `GooglePlacesProvider` the sole implementation, registered behind the `LOCATION_PROVIDER` DI token via `ProvidersModule`
- [x] `DiscoveryRunnerService` (Module M1) refactored to depend on `LOCATION_PROVIDER`, never the concrete `PlacesAdapter` — applying the "business logic must never directly depend on Google Places APIs" rule everywhere, per the founder's explicit confirmation, not just to new M5 code
- [x] `PlacesAdapter` extended: `searchNearby()` (coordinate/radius search), widened `FULL_DETAILS_FIELD_MASK` (phone, coordinates, opening hours, business status, Google Maps URL); no longer loops pages internally — `collectSearchPages()` (`common/utils/paginate-search.ts`) is the shared caller-owned page-loop used by both M1 and M5
- [x] Schema: `Business` extended with coordinates/phone/rating/reviewCount/openingHours/photos/businessStatus (promoted from the opaque `placesData` blob to typed columns) plus `googleBusinessUrl`, `websiteDetectedAt`, `websiteDetectionMethod`, `syncVersion`, `sourceProvider`, `lastSyncedAt`, `lastSyncJobId`; new `PlaceSyncJob` model (own model, not a reuse of M1's `DiscoveryJob`) with `startedAt`/`finishedAt`/`duration`/`successRate`/`apiCallsUsed`/`estimatedCost` plus per-run progress counters — DECISIONS.md D-033, D-034
- [x] Hand-authored migration (`packages/db/prisma/migrations/20260730010000_m5_place_sync/`) — purely additive (new enums, new nullable/defaulted columns, new table), no backfill needed, same no-live-DB constraint and `prisma migrate diff --from-schema-datamodel` technique as D-020/D-024/M4
- [x] `packages/shared-types`: `createPlaceSyncJobSchema` (city+category/keyword, or latitude+longitude — mirrors the `LocationProvider.search()` fork), `placeSyncJobSchema`, `PLACE_SYNC_JOB_STATUSES` (incl. `partial`), `LOCATION_SOURCE_PROVIDERS` (every named future provider, not just Google) — 13 unit tests
- [x] `BusinessService.upsertByPlaceId` extended: all M5 fields optional (M1 keeps working unchanged), `syncVersion` starts at 1 on create and atomically increments on update, `lastSyncedAt`/`websiteDetectedAt` stamped on every call
- [x] `PlaceSyncRunnerService` — the sync engine: retries (2 attempts, linear backoff) around every individual provider call; batch/bounded-concurrency candidate processing (shared `processWithConcurrency`, same as M1); progress persisted to the job row mid-run, not only at completion; job status resolves to `COMPLETED`/`PARTIAL`/`FAILED` based on per-candidate outcomes, not just search success
- [x] `PlaceSyncService`/`PlaceSyncController` — `POST/GET /place-sync-jobs`, `GET /place-sync-jobs/:id`; reuses Module M3's `discovery:run`/`discovery:read` permissions (no new permission strings), same cost-ceiling pre-flight check and `@Throttle` as `POST /discovery-jobs`
- [x] Unit tests: 60 new/updated tests (`google-places.provider.spec.ts`, `paginate-search.spec.ts`, `retry.spec.ts`, `place-sync-runner.service.spec.ts`, `place-sync.service.spec.ts`, `place-sync.controller.spec.ts`, `places.adapter.spec.ts` rewritten for the one-page-per-call contract, `discovery-runner.service.spec.ts` rewritten for the `LocationProvider` interface, `business.service.spec.ts` extended) — 282 total in `apps/api` (up from 245)
- [x] Integration tests: `apps/api/test/place-sync.e2e-spec.ts` — 11 tests covering auth, validation (city-vs-coordinates fork), RBAC, quota, and job-status reads; `discovery.e2e-spec.ts` updated for the new `PlacesAdapter` contract (82 e2e total, up from 71)
- [x] Full validation: monorepo typecheck/lint clean, full test suite green
- [ ] **Not done in this environment:** running the migration against a real Postgres — same D-020/D-024/D-029 constraint, explicitly flagged
- [ ] **Known limitation, flagged:** a coordinate-only sync (no `city` in the request) has no dedicated locality field from Google's search field mask — `Business.city` falls back to the full formatted address in that case
- [ ] **Deliberately out of scope, per instruction:** AI Business Analysis, Theme Selection, Website Generation, CRM UI, frontend — only Google Places synchronization was built
- **Status: Implementation complete, pending your approval.**

## M6 — AI Business Analyzer

Before any code was written, the founder required a complete AI architecture design (provider abstraction, prompt versioning, response validation, caching, retry/escalation, cost tracking) presented and approved. Two rounds of explicit requirements followed the initial plan (10 mandatory architecture requirements, then final schema/permission/cache-rule decisions) — DECISIONS.md D-037 through D-043.

- [x] `AiTextProvider` interface (`packages/ai/src/provider/`) — `complete()`, provider-agnostic; `AnthropicProvider` (`@anthropic-ai/sdk`) the sole implementation, registered behind the `AI_TEXT_PROVIDER` DI token via a new `AiModule` (`apps/api/src/common/ai/`) — mirrors M5's `LocationProvider`/`LOCATION_PROVIDER` pattern exactly
- [x] `packages/ai` gained a real build step (`tsc`, CommonJS) — was source-only through M0–M5, now consumable by future M7/M8 without an apps/api dependency
- [x] `PromptRegistry` + versioned prompt template (`packages/ai/src/prompt/business-analysis/v1.0.ts`) — `PROMPT_NAME`/`PROMPT_VERSION`/`PROMPT_HASH` (SHA-256 of the fixed instructional text, computed at module load so it can never drift from what actually ran); delimited `<business_data>` input block (prompt-injection guard against text embedded in reviews/business names)
- [x] `ResponseValidator` — Zod-based (`businessAnalysisOutputSchema` + `confidenceScore`, `packages/shared-types`), strips a stray markdown fence, never throws, returns a discriminated `{ok:true,data} | {ok:false,errors}` result
- [x] `AiService` gateway — the full retry/escalation ladder in one call: initial attempt → 1x same-model repair-prompt retry → 1x escalation to a stronger model (also with a repair prompt) → `FAILED`; each attempt wrapped in `withExponentialBackoff` (2 retries, 1s/2s) for transient failures, kept strictly separate from the validation-repair ladder; emits structured events (`retry_attempt`/`repair_prompt_sent`/`validation_failure`/`provider_error`) for the caller to log
- [x] Schema: `BusinessAnalysis` expanded from two opaque `Json` fields to typed columns — `analysisVersion` (unique per business, never overwritten), `promptName`/`promptVersion`/`promptHash`, `aiProvider`/`aiModel`, `inputHash` (the AI result cache key), `status` (`AnalysisStatus`: `PENDING`/`COMPLETED`/`FAILED`), `confidenceScore`, `rawResponse`/`validationErrors` (populated only on `FAILED`), `executionTimeMs`/`completedAt`, `promptTokens`/`completionTokens`/`totalTokens`/`estimatedCost`; `brandBrief` stays a single `Json` column (not promoted to typed columns — the whole 19-field value is read together, unlike Business's Places fields); two new enums (`AnalysisStatus`, `AiProviderName`) — DECISIONS.md D-038
- [x] Hand-authored, non-destructive migration (`packages/db/prisma/migrations/20260730020000_m6_ai_business_analyzer/`) — every new NOT NULL column added nullable first, backfilled with satisfiable sentinel values for any pre-M6 row, then constrained; `ai_model_used` dropped only after the backfill
- [x] AI result cache — `computeBusinessFingerprint()` (`apps/api/src/business-analysis/business-fingerprint.ts`), a SHA-256 of the founder's named fingerprint fields (name/category/rating/reviewCount/reviews via raw Places payload/website/phone/address/photos/`Business.syncVersion`); `BusinessAnalysisService.triggerAnalysis()` compares it against the latest `COMPLETED` analysis's `inputHash` — a match returns the cached analysis (200, no AI call, "Cache Hit" logged), a mismatch or no prior analysis creates a new `PENDING` row and dispatches the AI call (202, "Cache Miss" logged) — DECISIONS.md D-039
- [x] `BusinessAnalysisService`/`BusinessAnalysisRunnerService`/`BusinessAnalysisController` — `GET/POST /leads/:id/business` (Doc 19); `POST` is a dedicated new `business:analyze` permission (not a reuse of `leads:write` — D-043), fire-and-forget dispatch (D-004 precedent, same as M1/M5); runner persists `COMPLETED` (brandBrief + confidenceScore) or `FAILED` (rawResponse truncated to 64KB + validationErrors), reserves a conservative pre-flight cost estimate via `CostService.charge()` then records the real cost computed from actual token counts on the row itself
- [x] Unit tests: 24 new tests (`business-fingerprint.spec.ts`, `business-analysis.service.spec.ts`, `business-analysis-runner.service.spec.ts`, `business-analysis.controller.spec.ts`) plus 1 new `permission.constants.spec.ts` assertion — 307 total in `apps/api` (up from 282); `packages/ai` — 19 new tests (`response-validator.test.ts`, `retry.test.ts`, `ai.service.test.ts` incl. the full escalation ladder and a fake-timers transient-retry case, `anthropic.provider.test.ts`); `packages/shared-types` — 11 new tests (`business-analysis.test.ts`) — 76 total (up from 65)
- [x] Integration tests: `apps/api/test/business-analysis.e2e-spec.ts` — 11 tests covering auth, RBAC (`business:analyze` required for POST, `leads:read` sufficient for GET), cache hit/miss, `LEAD_NOT_FOUND`, and non-UUID validation — 93 e2e total (up from 82)
- [x] Full validation: monorepo typecheck/lint clean, full test suite green (307 unit + 93 e2e in `apps/api`; 19 in `packages/ai`; 76 in `packages/shared-types`), compiled server boots both with and without `ANTHROPIC_API_KEY` set (tolerant-at-boot, same convention as `GOOGLE_PLACES_API_KEY` — fails on actual use, not on startup)
- [ ] **Not done in this environment:** running the migration against a real Postgres — same D-020/D-024/D-029 constraint, explicitly flagged
- [ ] **Deliberately out of scope, per instruction:** website generation, theme selection, CRM UI, frontend — only business analysis was built
- **Status: Implementation complete, pending your approval.**

## M7 — Theme Engine

Before any code was written, the founder required a complete Theme Engine architecture (ThemeProvider abstraction, rule-based selection with AI recommendations, output field types) presented and approved. A follow-up message resolved the brand-field-sourcing/permission forks explicitly, added four required `ThemeConfiguration` fields, a compatibility-validation gate (`THEME_NOT_FOUND` on no match), and a full-ranking requirement (not just a single recommendation) — DECISIONS.md D-044 through D-047.

- [x] `ThemeProvider` interface + `THEME_PROVIDER` DI token (new `packages/themes/src/provider/`) — registry-shaped (unlike M5/M6's swap-shaped provider abstractions, every registered theme is simultaneously available); `StaticThemeRegistry` the sole implementation; 8 named theme definition modules (Restaurant/Salon/Dental/LawFirm/Gym/RealEstate/Medical/Corporate), each a versioned code module with name/version/hash (SHA-256, computed from content at load)/createdAt/updatedAt
- [x] New `packages/themes` package — mirrors `packages/ai`'s structure/build step (`tsc`, CommonJS), so it stays usable by M8 without an apps/api dependency
- [x] Compatibility scorer (`packages/themes/src/scoring/compatibility-scorer.ts`) — 5 weighted checks per theme (industry 40%, layout 20%, accessibility 15%, mobile 10%, component availability 15%), deterministic, no AI; `rankThemes()` scores every registered theme, filters to those clearing `MINIMUM_COMPATIBILITY_SCORE` (50), sorts descending — an empty result is the founder's explicit `THEME_NOT_FOUND` gate, never a forced bad fit
- [x] "AI recommends" step reuses M6's `AiService` gateway (`packages/ai`) — new `theme-recommendation/v1.0` prompt template + `AiService.recommendThemeCategory()`: a single-attempt, transient-retry-only classification (no repair-prompt ladder or model escalation — not warranted for a narrow 8-category classification), never throws, resolves to a `failed` outcome on any error so rule-based ranking always has a fallback path with zero AI dependency
- [x] Brand-identity lock (founder's explicit Decision 1) — `colorPalette`/`typography`/`layoutStyle`/`industry` flow through from `BusinessAnalysis.brandBrief` verbatim, never regenerated or overridden by theme selection; only structural/presentational fields (componentSet, navigationStyle, heroStyle, ctaStyle, cardStyle, footerStyle, animationLevel, imageStyle, sectionOrder, accessibilityProfile, mobilePreferences) come from the selected theme
- [x] Schema: new `ThemeConfiguration` model (7 new enums: NavigationStyle/HeroStyle/CtaStyle/CardStyle/FooterStyle/AnimationLevel/ImageStyle) — versioned per business like `BusinessAnalysis` (`configVersion`, never overwritten), FK to the exact `businessAnalysisId` it was derived from, plus the founder's required fields (`themeName`/`themeVersion`/`themeHash`/`selectedAt`/`selectedByEngineVersion`/`compatibilityScore`) and the full `rankedThemes` JSON array for future manual-override support — DECISIONS.md D-044
- [x] Purely additive migration (`packages/db/prisma/migrations/20260730030000_m7_theme_engine/`) — 7 new enums + 1 new table, no existing table altered
- [x] `ThemeSelectionService`/`ThemeSelectionController` — `GET/POST /leads/:id/theme`; new dedicated `theme:select` permission (not a reuse of `business:analyze` — founder's explicit Decision 2, same role set as `business:analyze`/`discovery:run`); synchronous (unlike M6's fire-and-forget dispatch — a lightweight single AI call plus deterministic scoring is fast enough to complete within the request); cached per `businessAnalysisId` (200 on a repeat call against the same analysis, 201 on a genuinely new selection)
- [x] **Post-review follow-up — CostService integration (DECISIONS.md D-048):** `ThemeSelectionService` now reserves `AI_THEME_RECOMMENDATION_ESTIMATED_COST_USD` ($0.02) via M6's `CostService.charge()` immediately before `recommendThemeCategory()`, and on success records actual `aiRecommendationProvider`/`aiRecommendationModel`/`aiRecommendationPromptTokens`/`aiRecommendationCompletionTokens`/`aiRecommendationTotalTokens`/`aiRecommendationCostUsd`/`aiRecommendationExecutionTimeMs` (7 new nullable `ThemeConfiguration` columns, folded into the not-yet-shipped M7 migration rather than a second migration). A monthly-ceiling rejection (`QuotaExceededException`) is caught by the exact same resilience path as any other AI failure — theme selection still succeeds, rules-only. The token→USD formula was extracted out of `BusinessAnalysisRunnerService` into a shared `apps/api/src/common/cost/ai-cost.util.ts` so M6 and M7 call one function, not two copies — no cost-tracking logic duplicated.
- [x] Unit tests: 24 new tests across `packages/themes` (registry, compatibility scorer incl. AI-boost blending and the Corporate floor), `packages/ai` (theme-recommendation validator + `AiService.recommendThemeCategory`, incl. the "never throws" contract under fake timers), and `apps/api` (`theme-selection.service.spec.ts` incl. the `THEME_NOT_FOUND` path and AI-failure resilience, `theme-selection.controller.spec.ts`, `theme-style.mapper.spec.ts`, +1 permission-matrix assertion), plus 4 more from the CostService follow-up (charge-before-call ordering, quota-exceeded resilience, cost/token/duration persistence on success, fields staying null on AI failure/skip) and 2 more `theme-configuration.test.ts` cases (nullable-vs-missing AI fields) — 335 total in `apps/api` (up from 307), 30 in `packages/ai` (up from 19), 17 in `packages/themes` (new), 89 in `packages/shared-types` (up from 76)
- [x] Integration tests: `apps/api/test/theme.e2e-spec.ts` — 13 tests covering auth, RBAC, cache hit/miss, `BUSINESS_ANALYSIS_NOT_FOUND`, the full AI-recommends+rules-rank flow against the real (unmocked) `StaticThemeRegistry`/compatibility scorer, non-UUID validation, and (added by the follow-up) the cost-ceiling-reached resilience path with `costEvent`/`incrementCounter` assertions on the successful path — 106 e2e total (up from 93)
- [x] Full validation: monorepo build/typecheck/lint clean across all packages/apps, full test suite green (apps/api 335 unit + 106 e2e, packages/shared-types 89, packages/ai 30, packages/themes 17, packages/db 26, packages/cache 11), compiled server boots
- [ ] **Not done in this environment:** running the migration against a real Postgres — same D-020/D-024/D-029/M5/M6 constraint, explicitly flagged
- [ ] **Deliberately out of scope, per instruction:** HTML generation, React components, complete websites, frontend, manual theme override — only a validated `ThemeConfiguration` is produced
- **Status: Implementation complete (incl. CostService follow-up), pending your approval.**

## M8 — Website Generator

The founder is building this module across four internal implementation phases (M8.1 Layout Generator, M8.2 Component Generator, M8.3 Content Binding, M8.4 React/Next.js Assembly) — phase boundaries only, not a roadmap change; the official roadmap (`docs/21-implementation-roadmap.md`) still lists M8 as a single module and is untouched by this phasing.

### Phase M8.1 — Layout Generator

Before any code was written, the founder specified a complete phase brief (objective, inputs/outputs, determinism requirement, validation list) — architecture was then presented (package placement, `LayoutConfiguration` shape, derivation rules, three open forks) and approved before implementation, same pre-implementation gate as every prior module. DECISIONS.md D-049, D-050+.

- [x] Extends M7's theme data model (`packages/themes`) with `sectionComponentMap: Record<sectionId, componentId[]>` on `ThemeDefinitionContent` — closes a real gap (componentSet/sectionOrder were two independent flat lists with no link between them) rather than a fragile name-matching heuristic, per the founder's resolved fork. Populated for all 8 existing theme definitions; a new `static-theme-registry.test.ts` invariant test asserts every componentSet entry is mapped exactly once, keyed only by real sections — DECISIONS.md D-049
- [x] `ThemeConfiguration` (M7) extended with `sectionComponentMap` (copied verbatim from the selected theme, same as every other theme-owned structural field) — amended directly into the still-unshipped M7 migration, same not-yet-shipped rationale as D-048
- [x] New `LayoutConfiguration` Prisma model — versioned per business like `BusinessAnalysis`/`ThemeConfiguration` (never overwritten), FK'd to both `businessAnalysisId` and `themeConfigurationId` for full provenance, every compound structure (pageStructure/navigation/hero/footer/sidebar/grid/responsiveRules/ctaPlacements/componentPlaceholders) stored as Json. New, purely additive migration (`20260731000000_m8_1_layout_generator`)
- [x] New `packages/website-generator` package (mirrors `packages/themes`/`packages/ai`'s structure — real `tsc`/CommonJS build, Prisma/NestJS-free) — `layout/layout-generator.ts`: `generateLayout(brandBrief, themeConfiguration)`, a pure, deterministic function (fixed lookup tables + arithmetic only, no AI call, no `Date.now()`/`Math.random()`, no I/O) producing page structure, navigation/hero/footer/sidebar layout, grid definitions, responsive rules, CTA placements, and component placeholders — structure only, no HTML/React/content/images generated, per the founder's explicit exclusions
- [x] `layout/layout-validator.ts`: `validateLayoutConfiguration()` — post-generation invariant checks (required sections exist, responsive rules exist, accessibility constraints, navigation integrity, section ordering). Founder's resolved fork: since inputs are already M6/M7-validated and every rule is a fixed lookup, a failure here is an internal bug, not a business outcome — throws a plain `Error`, never a new domain exception/HTTP code (unlike M7's `THEME_NOT_FOUND`)
- [x] `apps/api/src/layout-engine/` — `LayoutGenerationService`/`LayoutGenerationController`, `GET/POST /leads/:id/layout`, new dedicated `layout:generate` permission (founder's resolved fork: ship the endpoint this phase, same role set as `theme:select`/`business:analyze`), cached per `themeConfigurationId`; new `ThemeConfigurationNotFoundException` (`THEME_CONFIGURATION_NOT_FOUND`, 404) for the hard M7 dependency
- [x] Unit tests: 28 new in `packages/website-generator` (18 generator incl. a determinism assertion, 10 validator), 12 new in `apps/api` (8 service, 4 controller), 1 new invariant test in `packages/themes`, 13 new in `packages/shared-types` (`layout-configuration.test.ts`) — 347 total in `apps/api` (up from 335), 18 in `packages/themes` (up from 17), 28 in `packages/website-generator` (new package), 102 in `packages/shared-types` (up from 89)
- [x] Integration tests: `apps/api/test/layout.e2e-spec.ts` — 12 tests covering auth, RBAC, cache hit/miss, `THEME_CONFIGURATION_NOT_FOUND`, the full real (unmocked) `generateLayout()`/`validateLayoutConfiguration()` flow, and non-UUID validation — 118 e2e total (up from 106)
- [x] Full validation: monorepo build/typecheck/lint clean across all packages/apps, full test suite green
- [ ] **Not done in this environment:** running the migration against a real Postgres — same constraint as every prior module
- [ ] **Deliberately out of scope, per instruction:** component generation, content binding, React generation, Next.js generation — those are M8.2/M8.3/M8.4
- **Status: Phase M8.1 implementation complete — founder proceeded to Phase M8.2.**

### Phase M8.2 — Component Generator

Founder specified a complete phase brief (objective, inputs/outputs, determinism + no-hallucination requirements, validation list), followed by an explicit Theme Tokens requirement with concrete examples (`token.primary`, `token.radius.large`, `token.heading`, `token.spacing.md`) — architecture was then presented (package placement, `ComponentManifest`/`ThemeTokens` shape, derivation rules, two open forks) and approved before implementation. DECISIONS.md D-055 through D-060.

- [x] Global `COMPONENT_TYPE_REGISTRY` (`packages/themes/src/provider/component-type-registry.ts`) — classifies all 31 theme-authored componentIds across the 8 M7 themes onto a closed 16-value `SupportedComponentType` taxonomy (3 structural: navigation/section/sidebar; 13 theme-owned). Single global lookup, not per-theme (unlike M8.1's sectionComponentMap) — a componentId means the same thing in every theme. New registry test asserts full coverage — DECISIONS.md D-055
- [x] New `ComponentManifest`/`ThemeTokens`/`ComponentDefinition` schema (`packages/shared-types/src/component-manifest.ts`) — closed enums for component type, content-slot kind, accessibility role, visibility condition (only literals the algorithm actually produces, same discipline as D-051)
- [x] `packages/website-generator/src/component/` — `component-tokens.ts`: `computeThemeTokens()`, a pure deterministic derivation (colors/fonts verbatim from ThemeConfiguration, fixed radius/spacing scales, cardStyle-derived shadow). `component-type-definitions.ts`: hand-authored `COMPONENT_TYPE_DEFINITIONS` table (requiredContent/optionalContent/accessibility role/default token references) per supported type, same "authored data, not generated" discipline as M7's 8 theme definitions
- [x] `component-generator.ts`: `generateComponentManifest(brandBrief, themeConfiguration, layoutConfiguration)` — pure, deterministic, 2-level hierarchy (section wrapper → mapped theme component; root-level navigation; optional sidebar when populated). Throws if a componentId isn't classified in COMPONENT_TYPE_REGISTRY — the "no hallucinated components" guard. Theme Tokens referenced by path string only (e.g. `token.radius.large`), never a resolved value copied onto a component — founder's explicit requirement
- [x] `component-validator.ts`: `validateComponentManifest()` — hierarchy/parent-child integrity (bidirectional), required component dependencies (exactly one navigation; sidebar visibility tied to its own presence), accessibility metadata, responsive compatibility (monotonic grid columns). Same internal-assertion semantics as M8.1's validator (D-052/D-060) — plain `Error`, not a new domain exception
- [x] New `ComponentManifest` Prisma model — versioned per business, triple-FK'd (businessAnalysisId/themeConfigurationId/layoutConfigurationId) for full provenance, `themeTokens`/`components` stored as Json. New, purely additive migration (`20260801000000_m8_2_component_generator`)
- [x] `apps/api/src/component-engine/` — `ComponentGenerationService`/`ComponentGenerationController`, `GET/POST /leads/:id/components`, new dedicated `component:generate` permission (same role set as `layout:generate`/`theme:select` — not re-litigated as a fork, ship-endpoint-this-phase precedent already established by M6/M7/M8.1), cached per `layoutConfigurationId`; new `LayoutConfigurationNotFoundException` (`LAYOUT_CONFIGURATION_NOT_FOUND`, 404) for the hard M8.1 dependency
- [x] Unit tests: 24 new in `packages/website-generator` (14 generator incl. a determinism assertion and an unsupported-component-throws case, 10 validator), 12 new in `apps/api` (8 service, 4 controller), 1 new registry test in `packages/themes`, 15 new in `packages/shared-types` (`component-manifest.test.ts`) — 359 total in `apps/api` (up from 347), 19 in `packages/themes` (up from 18), 52 in `packages/website-generator` (up from 28), 117 in `packages/shared-types` (up from 102)
- [x] Integration tests: `apps/api/test/component.e2e-spec.ts` — 12 tests covering auth, RBAC, cache hit/miss, `LAYOUT_CONFIGURATION_NOT_FOUND`, the full real (unmocked) `generateComponentManifest()`/`validateComponentManifest()` flow, and non-UUID validation — 130 e2e total (up from 118)
- [x] Full validation: monorepo build/typecheck/lint clean across all packages/apps, full test suite green; live boot smoke test confirms `GET /leads/:id/components` is wired and auth-gated
- [ ] **Not done in this environment:** running the migration against a real Postgres — same constraint as every prior module
- [ ] **Deliberately out of scope, per instruction:** React code, Next.js pages, HTML, CSS, Tailwind classes, website content, images — those are M8.3/M8.4 or explicitly excluded
- **Status: Phase M8.2 implementation complete — founder proceeded to Phase M8.3.**

### Phase M8.3 — Content Binding

Founder specified a complete phase brief (objective, inputs/outputs, no-AI/deterministic-mapping requirement, validation list, "every field traceable to its origin" requirement with a concrete `{value, source}` example) — architecture was then presented (package placement, `ContentManifest` shape, binding rules, three open forks) and approved before implementation. Two additional data-model gaps (Business as a necessary 5th input; real Google review text deliberately not used) were found and resolved without re-blocking. DECISIONS.md D-061 through D-067.

- [x] New `ContentManifest`/`SourcedValue`/`BusinessContactInfo` schema (`packages/shared-types/src/content-manifest.ts`) — five concrete "sourced value" shapes (text, text list, link, link list, image reference), each pairing a real value with its exact upstream field
- [x] `packages/website-generator/src/content/` — `content-binding-rules.ts`: one resolver per (componentType, slotName) pair with a real deterministic source, hand-authored, same "authored data, not generated" discipline as M7/M8.2. `seo-binder.ts`: keywords/localSeoSuggestions verbatim, metaDescription verbatim, metaTitle via a fixed template over real fields; LocalBusiness/Organization/BreadcrumbList/FAQPage(when-applicable) structured-data mapping
- [x] `content-generator.ts`: `generateContentManifest()` — pure, deterministic, binds only the content slots M8.2's ComponentManifest already declared, never invents a new slot. A required slot with genuinely no upstream source is recorded in `unresolvedBindings[]`, never fabricated (founder's resolved fork) — the manifest still validates successfully
- [x] `content-validator.ts`: `validateContentManifest()` — no orphan bindings, no duplicated bindings, no slot both bound and unresolved, every required slot bound-or-unresolved, CTA targets exist, SEO/schema.org completeness. Same internal-assertion semantics as M8.1/M8.2's validators (D-052/D-060), carried forward without re-asking
- [x] New `ContentManifest` Prisma model — versioned per business, quad-FK'd (businessAnalysisId/themeConfigurationId/layoutConfigurationId/componentManifestId) for full provenance, all four compound fields stored as Json. New, purely additive migration (`20260802000000_m8_3_content_binding`)
- [x] `apps/api/src/content-engine/` — `ContentBindingService`/`ContentBindingController`, `GET/POST /leads/:id/content`, new dedicated `content:bind` permission (same role set as `component:generate`/`layout:generate`/`theme:select`), cached per `componentManifestId`; new `ComponentManifestNotFoundException` (`COMPONENT_MANIFEST_NOT_FOUND`, 404) for the hard M8.2 dependency; new `toBusinessContactInfo()` mapper (Business Prisma row → the minimal contact-info shape this phase needs)
- [x] Unit tests: 21 new in `packages/website-generator` (11 generator incl. determinism + required-slot-unresolved cases, 10 validator), 13 new in `apps/api` (10 service, 3 controller), 15 new in `packages/shared-types` (`content-manifest.test.ts`) — 372 total in `apps/api` (up from 359), 73 in `packages/website-generator` (up from 52), 132 in `packages/shared-types` (up from 117)
- [x] Integration tests: `apps/api/test/content.e2e-spec.ts` — 12 tests covering auth, RBAC, cache hit/miss, `COMPONENT_MANIFEST_NOT_FOUND`, the full real (unmocked) `generateContentManifest()`/`validateContentManifest()` flow with a per-field origin-traceability assertion, and non-UUID validation — 142 e2e total (up from 130)
- [x] Full validation: monorepo build/typecheck/lint clean across all packages/apps, full test suite green; live boot smoke test confirms `GET /leads/:id/content` is wired and auth-gated
- [ ] **Not done in this environment:** running the migration against a real Postgres — same constraint as every prior module
- [ ] **Deliberately out of scope, per instruction:** AI calls, prompt execution, LLM providers, content regeneration, images, HTML, React, CSS, Next.js — those are M8.4 or explicitly excluded
- **Status: Phase M8.3 implementation complete — founder proceeded to Phase M8.4.**

### Phase M8.4 — React/Next.js Website Assembly

Founder specified a complete phase brief (objective, inputs, full technology stack, accessibility/SEO/performance requirements, validation list, "must build and run with zero manual edits" requirement), followed by an explicit **Additional Architecture Requirement**: "The Website Assembly Engine must use templates... Never concatenate JSX strings... Use structured template rendering." Architecture was then presented resolving that requirement (static templates + TypeScript Compiler API for the two per-business-varying files) with two open forks (persistence model, component breadth) approved before implementation. DECISIONS.md D-068 through D-074.

- [x] `packages/website-generator/templates/nextjs-base/` — a real, static, production-quality Next.js App Router project: config files (`package.json`/`tsconfig.json`/`next.config.js`/`tailwind.config.ts`/`postcss.config.js`), `app/layout.tsx` (skip-link, `metadataBase`), shadcn/ui primitives (Button/Card/Badge/Separator/Input/Label/Accordion), `lib/` helpers (`cn()`, `resolvePlacePhotoUrl()` gated on a deploy-time `NEXT_PUBLIC_GOOGLE_PLACES_API_KEY` env var, `gridColumnsClassName()` fixed lookup table — fixes a real Tailwind JIT bug where dynamically-interpolated `grid-cols-${n}` classes are invisible to the build-time scanner), standalone `lib/types.ts` (no workspace dependency on `@riznexia/shared-types` — the generated project is fully independent). Excluded from this package's own `tsc`/`eslint` (outside `tsconfig.json`'s `include`; `templates/**` added to `eslint.config.mjs`'s `ignores` — Next.js App Router's own conventions require default exports, which conflict with this monorepo's own "named exports only" standard)
- [x] 16 real, accessible section components (`components/sections/`), one per `SupportedComponentType` — semantic HTML, WCAG AA (keyboard-operable disclosures, `aria-live` regions, real `<table>`/`<form>` markup), `prefers-reduced-motion`-aware Framer Motion, each gracefully rendering `null` when its content is genuinely absent (no data source exists yet for FAQ/schedule/pricing/logos/profiles) rather than a fake placeholder. `map-embed.tsx` renders an honest external Google Maps link, never a broken/misleading `<iframe>` (no signed Maps Embed API call exists in this pipeline)
- [x] `packages/website-generator/src/assembly/` — `ts-ast-helpers.ts` (the single controlled `valueToExpression()` JS-value-to-AST serializer, plus JSX/import/typed-const factory helpers — the "structured template rendering" primitive every generator below is built on), `component-template-registry.ts` (fixed `SupportedComponentType -> {componentName, propsTypeName, importPath}` lookup), `component-props-mapper.ts` (merges each component's bound `ContentManifest` fields with structural/theme-derived values into one props object per component, computed at generation time), `site-data-generator.ts` (`lib/site-data.ts` — one type-annotated `export const <id>Props: <Type> = {...}` per component, not a single `as const` blob — DECISIONS.md D-069), `page-ast-generator.ts` (`app/page.tsx` — `Metadata` export, JSON-LD `<script>` tags via `jsonld-mapper.ts`, `<main id="main-content">` wrapping every section in manifest order, footer/sidebar handled specially), `theme-tokens-css-generator.ts` (`app/theme-tokens.css`), `seo-files-generator.ts` (`app/robots.ts`/`app/sitemap.ts`/`app/manifest.ts`), `package-json-generator.ts` (JSON.parse → mutate → JSON.stringify, never string-replace), `static-template-loader.ts` (recursive, sorted, deterministic template read), `website-assembler.ts` (`assembleWebsite()` orchestration, `ASSEMBLY_ENGINE_VERSION`), `website-validator.ts` (`validateWebsiteAssembly()`)
- [x] `generateContentManifest()`/`generateComponentManifest()`/`generateLayout()` are never called from assembly code — `assembleWebsite()` only reads the four already-produced manifests (`ThemeConfiguration`/`LayoutConfiguration`/`ComponentManifest`/`ContentManifest`); `businessName` is read back out of `ContentManifest.structuredData` rather than requiring a 6th `Business` input — DECISIONS.md D-071
- [x] New `GeneratedWebsite`/`GeneratedWebsiteFile` schema (`packages/shared-types/src/generated-website.ts`) and Prisma model — versioned per business, quintuple-FK'd (businessAnalysisId/themeConfigurationId/layoutConfigurationId/componentManifestId/contentManifestId) for full provenance, `files: Json`. New, purely additive migration (`20260803000000_m8_4_website_assembly`) — the pre-existing `Website`/`WebsitePage`/`Deployment` roadmap scaffolding is untouched, not repurposed — DECISIONS.md D-072
- [x] `apps/api/src/website-assembly/` — `WebsiteAssemblyService`/`WebsiteAssemblyController`, `GET/POST /leads/:id/website`, new dedicated `website:assemble` permission (same role set as `content:bind`/`component:generate`/`layout:generate`/`theme:select`), cached per `contentManifestId`; new `ContentManifestNotFoundException` (`CONTENT_MANIFEST_NOT_FOUND`, 404) for the hard M8.3 dependency. No `BusinessModule` import (unlike `ContentEngineModule`) — the service never touches the raw `Business` record
- [x] Unit tests: 7 new in `packages/website-generator` (`website-assembler.test.ts` — determinism, required files present, package.json naming, valid page.tsx/site-data.ts, JSON-LD present and valid JSON, passes `validateWebsiteAssembly()`, theme-tokens.css custom-property coverage), 12 new in `apps/api` (8 service, 4 controller), 8 new in `packages/shared-types` (`generated-website.test.ts`) — 384 total in `apps/api` (up from 372), 80 in `packages/website-generator` (up from 73), 140 in `packages/shared-types` (up from 132)
- [x] Integration tests: `apps/api/test/website.e2e-spec.ts` — 12 tests covering auth, RBAC, cache hit/miss, `CONTENT_MANIFEST_NOT_FOUND`, the full real (unmocked) `assembleWebsite()`/`validateWebsiteAssembly()` flow (asserting real generated file paths, `package.json` naming, and `page.tsx` content), and non-UUID validation — 154 e2e total (up from 142)
- [x] **Real, unmocked `npm install && npm run build && npm run dev` smoke test** against a materialized fixture-driven project — substantiates the "must build and run with zero manual edits" requirement beyond what unit/e2e tests alone can verify (they never invoke Next.js's own compiler/bundler against the generated project's own source). Caught one real bug (`SectionWrapperProps.children` needed to be optional) invisible to all 87 assembly-related automated tests — fixed, then reverified: `npm install` (131 packages, zero errors), `npm run build` (compiled, typechecked, linted, statically generated all 6 routes including `robots.txt`/`sitemap.xml`/`manifest.webmanifest`, zero errors), `npm run dev` (served a real `200` with the `main-content` landmark and JSON-LD present) — DECISIONS.md D-074
- [x] Full validation: monorepo build/typecheck/lint clean across all packages/apps, full test suite green
- [ ] **Not done in this environment:** running the migration against a real Postgres — same constraint as every prior module
- [ ] **Known limitation, flagged not hidden:** `aggregateRating` in `LocalBusiness` JSON-LD is emitted as a plain human-readable string, not a proper schema.org `AggregateRating` object — the assembler only has M8.3's already-combined "4.5/5 (120 reviews)" string, not the original numeric `rating`/`reviewCount` fields
- **Status: Phase M8.4 implementation complete, pending your approval. Module M8 (all four phases) complete pending approval.**

## M9 — Website Preview

Founder specified a complete module brief (objective, inputs [GeneratedWebsite/BusinessAnalysis/ThemeConfiguration], outputs [WebsitePreview/PreviewReport/PublishReadinessReport], device preview modes, the full validation checklist across four categories, per-rule field requirements, deterministic-only performance readiness, constraints [never regenerate/modify/deploy]) — architecture was presented (package placement, validator taxonomy, the rendering-mechanism fork) and approved, followed by four explicit founder Decisions refining it before implementation: (1) Preview Immutability — fully read-only, never modifies GeneratedWebsite; (2) Cache Strategy — regenerate only when GeneratedWebsite.configVersion changes, otherwise return cached; (3) Validation Architecture — validators never call each other, PublishReadinessEngine only aggregates, never validates; (4) Permission — dedicated `website:preview`, not a reuse of `website:assemble`. DECISIONS.md D-075 through D-081.

- [x] **Rendering-mechanism fork resolved (AskUserQuestion):** structural/data preview only this phase — `WebsitePreview` returns a file manifest, business/theme summary, and fixed desktop/tablet/mobile viewport presets; no live process, no execution of generated code, zero new infrastructure. Live visual rendering explicitly deferred to a future infra decision — DECISIONS.md D-076
- [x] New `packages/website-preview` package (its own new module package, not folded into `packages/website-generator` — structurally guarantees zero import of any generator function, enforcing "never regenerates" at the package-boundary level) — `ast-inspect-helpers.ts` (the read-only mirror of website-generator's `ts-ast-helpers.ts`: `ts.createSourceFile()` + a visitor, never `ts.factory`), `color-contrast.ts` (a real WCAG relative-luminance contrast-ratio calculation, not a heuristic)
- [x] Five independent validators (`validation/`), each a pure function over `{files, businessAnalysis, themeConfiguration}`, each owning exactly one `RuleCategory`, none importing another (founder's Decision 3): `structuralValidator` (file/asset/route/link/image existence — the "Validation Engine" checklist), `contentCompletenessValidator` (every M8.3-bound field is non-empty), `seoValidator` (title/description/keywords/canonical/OpenGraph/Twitter/JSON-LD LocalBusiness/Organization/FAQ/Breadcrumb value-completeness), `accessibilityValidator` (skip-link, main/nav landmarks, alt text, heading hierarchy, keyboard-operable disclosure, real WCAG AA contrast ratio, focus-visibility, WCAG 2.5.5 touch target size), `performanceValidator` (next/image usage, priority loading, client-bundle-size proxy, static-generation eligibility, prefers-reduced-motion respect — deterministic proxies only, no Lighthouse, per explicit instruction)
- [x] `validation/validator-registry.ts` — `VALIDATOR_REGISTRY: WebsiteValidator[]` + `runAllValidators()`, the founder's explicit Future Compatibility extensibility point: a future validator (Security/Legal/Cookie/Brand/Broken-Link, per the founder's own examples) is one new file + one array entry — no existing validator file changes, verified by a test that registers a synthetic validator and confirms every existing validator's own output is unaffected
- [x] `preview/preview-generator.ts` — `generateWebsitePreview()`: businessName read back out of the generated website's own JSON-LD (same "read it back out of structured data" technique as M8.4's D-071, since M9 has no direct `Business` input), fixed device presets, sorted file-size manifest. `readiness/publish-readiness-engine.ts` — `aggregateReadiness()`: pure fold over an already-computed `ValidationRuleResult[]`, never touches `GeneratedWebsite.files` or calls a validator (founder's Decision 3); each rule in a category is an equal share of 100 points (warning costs half, error costs all), every score below 100 carries a `deductions[]` list naming exactly which rules cost how many points and why (founder's explicit "must explain why points were deducted" requirement); Overall Publish Score is a fixed-weight average (SEO 20% / Accessibility 25% / Performance 15% / Content Completeness 20% / Structural Integrity 20%)
- [x] New `WebsitePreview`/`PreviewReport`/`PublishReadinessReport` schemas (`packages/shared-types/src/website-preview.ts`) and Prisma models — each independently versioned per business (own `previewVersion` sequence), each carrying the same 5-field metadata envelope the founder required (`previewVersion`/`generatedWebsiteVersion`/`validationVersion`/`generatedByModuleVersion`/`createdAt`), FK'd to `businessId`+`generatedWebsiteId` only (not chained to each other — `PublishReadinessReport` has no FK to `PreviewReport`, since it's generated independently, founder's Decision 1). New, purely additive migration (`20260804000000_m9_website_preview`)
- [x] `apps/api/src/website-preview/` — `WebsitePreviewService`/`WebsitePreviewController`, `GET /leads/:id/preview` + `/preview/validation` + `/preview/readiness` — no POST anywhere in this module (unlike every M6-M8 phase): validation has no external cost to gate behind an explicit generate step, so each GET computes-and-caches itself, keyed to `GeneratedWebsite.configVersion` (founder's Decision 2). All three gated by the new dedicated `website:preview` permission (not `leads:read`, unlike M6-M8's own GETs — viewing a preview/report is itself the privileged action here, since a GET triggers real computation). New `GeneratedWebsiteNotFoundException` (`GENERATED_WEBSITE_NOT_FOUND`, 404) for the hard M8.4 dependency. No `BusinessModule` dependency (same reasoning as `WebsiteAssemblyModule`)
- [x] Structured logging: "Preview Opened" / "Validation Started" / "Validation Completed" / "Publish Readiness Generated" (founder's explicit logging requirement), each on both the cache-hit and cache-miss path
- [x] Unit tests: 69 new in `packages/website-preview` (7 color-contrast incl. real WCAG threshold assertions, 9 structural, 6 content-completeness, 8 SEO, 9 accessibility, 7 performance, 6 registry incl. a Future-Compatibility extensibility test, 7 preview-generator, 10 readiness-engine incl. hand-computed score-math verification), 15 new in `apps/api` (11 service, 3 controller, 1 permission-parity), 15 new in `packages/shared-types` (`website-preview.test.ts`) — 399 total in `apps/api` (up from 384), 69 in `packages/website-preview` (new package), 155 in `packages/shared-types` (up from 140)
- [x] Integration tests: `apps/api/test/preview.e2e-spec.ts` — 12 tests covering auth, RBAC (all three GETs gated by `website:preview`, not `leads:read`), `GENERATED_WEBSITE_NOT_FOUND`, the full real (unmocked) `generateWebsitePreview()`/`runAllValidators()`/`aggregateReadiness()` flow against a real assembled website, cache hit/miss, an explicit assertion that `/readiness` never queries `PreviewReport` (proving independence, founder's Decision 1/3), and non-UUID validation — 166 e2e total (up from 154)
- [x] A real bug found and fixed during implementation: the initial `STRUCT-009`/broken-internal-link check scanned `app/page.tsx`'s literal JSX `href`/`id` attributes — but M8.4's real templates spread `{...props}` (including `id`) and build `href` from a runtime template literal, so no such literal attributes exist in the real generated output, making the original check vacuously always-pass. Fixed by reading `id`/`targetComponentId` directly out of `lib/site-data.ts`'s already-resolved props (the same source the real values actually live in) — caught by a determinism/negative-path test against a real assembled fixture, not assumed correct from reading the generator's own source
- [x] Full validation: monorepo build/typecheck/lint/test clean across all packages/apps
- [ ] **Not done in this environment:** running the migration against a real Postgres — same constraint as every prior module
- [ ] **Known limitation, flagged not hidden:** live visual rendering (an actual running preview a user can look at) is out of scope this phase (D-076) — `WebsitePreview` is a structural/data manifest only
- [ ] **Known limitation, flagged not hidden:** a future validator with a genuinely new `RuleCategory` (e.g. `security`) contributes its rules to `PreviewReport` but doesn't automatically get its own named score in `PublishReadinessReport` — that requires a small, separate future update to `PublishReadinessEngine`'s five named fields, by design (the founder's "PublishReadinessEngine only aggregates" constraint means it can't infer a sensible new named field on its own)
- **Status: Module M9 implementation complete, pending your approval.**

## M10 — Sales CRM

Founder specified a complete module brief (objective — "manage the complete lifecycle of selling AI-generated websites... NOT a generic CRM," a discover→demo→deploy workflow diagram, core features [Opportunity Pipeline, Sales Stages, Follow-ups, Tasks, Reminders, Activity Timeline, Notes, Proposal Tracking, Website Status, Lead Assignment], a default 10-stage pipeline, activity types, task/proposal field lists, dashboard metrics, four dedicated permissions, five named "separate modules" [Pipeline/Task/Activity/Proposal/Reporting Engines], Future Compatibility reservations, and explicit constraints [no email/WhatsApp sending, no deployment, no analytics, no AI]). A roadmap-vs-brief scope conflict (`docs/21`'s M10 entry describes an unrelated AI Sales Agent module) was flagged via AskUserQuestion and resolved before any code was written — the founder's live brief and this conversation's approved architecture are the implementation source of truth (DECISIONS.md D-082), `docs/21` itself untouched. An architecture was presented and approved with twelve founder Decisions refining it before implementation. DECISIONS.md D-082 through D-091.

- [x] **Prisma schema**: `SalesStage`/`LostReason` (configurable tables, seeded with 10 stages/7 reasons by the migration itself, `archivedAt` — archive not delete), `LeadCRM` (1:1 with `Lead` via unique FK, lazily created — `stage`/`dealValueUsd`/`lostReasonId`/`ownerId`/`nextFollowUpAt`/`lastActivityAt`), `CrmTask` (`estimatedDurationMinutes`/`actualDurationMinutes`/`completedById` per founder's Decision 4), `CrmSettings` (seeded singleton), `SalesProposal` rewritten for immutable per-version rows (`content` nullable, `@@unique([leadId, version])`). `LeadActivityType` extended with `CALL`/`EMAIL`/`MEETING`/`WHATSAPP`/`WEBSITE_GENERATED`/`PREVIEW_SENT`/`PROPOSAL_SENT` (reused, not duplicated — founder's Decision 5). New migration `20260805000000_m10_sales_crm` — DECISIONS.md D-083, D-084, D-085, D-086, D-087, D-091
- [x] `packages/shared-types` — 9 new schema files (`sales-stage`/`lost-reason`/`lead-crm`/`crm-task`/`sales-proposal`/`crm-settings`/`website-status`/`dashboard-stats`/`crm-activity`), each with its own `.test.ts`. `website-status.ts` deliberately named `WebsiteGenerationStatus` (not `WebsiteStatus`) to avoid colliding with the pre-existing, unrelated M1/M5 `Business.websiteStatus` export — DECISIONS.md D-088
- [x] `apps/api/src/common/exceptions/app.exception.ts` — `SalesStageNotFoundException`/`LostReasonNotFoundException`/`CrmTaskNotFoundException`/`SalesProposalNotFoundException`/`LostReasonRequiredException`. `apps/api/src/common/rbac/permission.constants.ts` — four new permissions (`crm:view`/`crm:manage`/`crm:assign`/`crm:report`); `admin`/`sales_manager` hold all four, `sales_executive` holds only `crm:view`/`crm:manage`, `developer`/`viewer` hold none — DECISIONS.md D-090
- [x] **Pipeline Engine** (`apps/api/src/crm/pipeline/`) — `SalesStageService`/`LostReasonService`/`CrmSettingsService`/`WebsiteStatusService`/`LeadCrmService`, five controllers. `LeadCrmService.getOrCreate()` lazily creates a lead's `LeadCRM` row on first CRM touch, defaulting to `CrmSettings.defaultStageId` or the lowest-`order` non-archived stage. `transitionStage()` requires (and validates) a `lostReasonId` when the target stage `isLost`, clears it otherwise, and logs `STAGE_CHANGED`; `assignOwner()` validates the target `TeamMember` and logs `ASSIGNED`/`UNASSIGNED` — both wrapped in a `$transaction` with the `LeadCRM` write. `WebsiteStatusService.getForLead()` computes `WebsiteGenerationStatus` fresh from 8 parallel M6-M9 existence-checks, nothing stored — gated `leads:read`, not `crm:view` (DECISIONS.md D-088). All five services exported for `ReportingModule`'s future use (DECISIONS.md D-089)
- [x] **Task Engine** (`apps/api/src/crm/tasks/`) — `CrmTaskService` (per-lead `POST /leads/:id/tasks` + `GET`, cross-lead `GET/PATCH /crm/tasks[/:id]`). `status: 'completed'` idempotently stamps `completedAt`/`completedById` (not re-stamped on a repeat PATCH); moving status away from `completed` clears both. `CANCELLED` status serves as the archive-equivalent for tasks — no separate delete endpoint
- [x] **Activity Engine** (`apps/api/src/crm/activities/`) — `CrmActivityService`, `POST /leads/:id/activity` restricted to the four manually-loggable interaction types (call/email/meeting/whatsapp — Notes already has its own M4 endpoint, system-emitted types are never manually logged). Reuses `LeadActivityService` (now exported from `LeadsModule`) and keeps `LeadCRM.lastActivityAt` current inside the same transaction as every activity write
- [x] **Proposal Engine** (`apps/api/src/crm/proposals/`) — `SalesProposalService`, `POST /leads/:id/proposals` (server-computed `version = max + 1`, never client-supplied), `GET` list/detail, `PATCH .../:proposalId` (status/tracking fields only — the request schema has no `content` key at all, so an injected `content` field is silently stripped before it ever reaches the service, verified by an e2e test). `status: 'sent_manually'` stamps `sentAt`, logs `PROPOSAL_SENT`, and touches `LeadCRM.lastActivityAt` — DECISIONS.md D-085
- [x] **Reporting Engine** (`apps/api/src/crm/reporting/`) — `ReportingService.getDashboardStats()`, `GET /crm/dashboard` gated on the dedicated `crm:report` permission. Reads `LeadCRM` joined with `SalesStage`/`LostReason`/`TeamMember` directly (not through per-entity service methods, which have no bulk-list surface) but every business judgment it makes is a plain read of a field the Pipeline Engine already computed (`isWon`/`isLost`/`label`/`name`), never re-derived — founder's Decision 6. Computes `pipelineValueByStage`/`totalPipelineValueUsd` (open leads only), `conversionRatePercent` (won ÷ all), `winRatePercent` (won ÷ won+lost), `averageSalesCycleDays` (from the most recent matching `STAGE_CHANGED` activity, falling back to `updatedAt`), `lostReasonsBreakdown`, `salesPerformanceByRep` — a pure, fully-serializable function of `(dateRange, filters)`, cache-ready per founder's Decision 9 though no caching is wired this phase — DECISIONS.md D-089
- [x] All five engines registered in `apps/api/src/app.module.ts`; verified by running the full `AppModule` (e2e) test, which compiles and boots the entire DI graph
- [x] Unit tests: 105 new in `apps/api` (49 Pipeline, 21 Task, 6 Activity, 16 Proposal, 10 Reporting, across 20 spec files — services and controllers), 61 new in `packages/shared-types` (9 new `.test.ts` files) — 504 total in `apps/api` (up from 399), 216 in `packages/shared-types` (up from 155)
- [x] Integration tests: `apps/api/test/crm.e2e-spec.ts` — 17 tests covering all five engines' guard/permission/validation-pipe/controller/service wiring, RBAC (crm:view vs crm:manage vs crm:assign vs crm:report, each independently gated), the lost-reason-required stage-transition error, and the proposal immutability trust boundary (an injected `content` field never reaches the Prisma `update()` call) — 183 e2e total (up from 166)
- [x] Full validation: monorepo build/lint/typecheck/test clean across every package and app (the two zero-test packages, `packages/ui` and `apps/web`, and `packages/logger`, are pre-existing and unrelated to this module)
- [ ] **Not done in this environment:** running the migration against a real Postgres — same constraint as every prior module
- [ ] **Known limitation, flagged not hidden:** `averageSalesCycleDays` is an approximation — no dedicated `wonAt` timestamp is stored anywhere; it's derived from the most recent `STAGE_CHANGED` activity matching a lead's current won stage, falling back to `LeadCRM.updatedAt` when no such activity exists (e.g. seeded data)
- [ ] **Known limitation, flagged not hidden:** no caching is actually wired for `GET /crm/dashboard` this phase — `DashboardStats`' shape is designed to support it (founder's Decision 9), but every request recomputes from scratch
- [ ] **Deliberately out of scope, per instruction:** email sending, WhatsApp sending, deployment, analytics, AI chatbots, marketing automation — this module only manages the sales workflow
- **Status: Module M10 implementation complete, pending your approval.**

## M11 — Deployment

Founder specified a complete module brief (objective — publish generated websites to production; must never generate or modify them; inputs `GeneratedWebsite` M8/`PublishReadinessReport` M9; a provider-agnostic Deployment Provider abstraction naming six future providers; the full lifecycle Requested→Build Validation→Started→Completed→Health Check→Production Ready with a Failed/Retry branch; immutable deployment history; rollback-as-a-new-deployment; domain/SSL metadata management with no DNS automation; deterministic health checks; four dedicated permissions; the exact `WebsiteDeployment` field list; five named independent engines [Deployment Engine, Deployment Provider, Health Check Engine, Rollback Engine, Domain Engine]; Future Compatibility reservations; explicit constraints [no website generation/modification]). A naming collision with the pre-existing, unused M2 `Deployment`/`Website` scaffold was resolved the same way M10's roadmap conflict was (a new `WebsiteDeployment` model, FK'd to `GeneratedWebsite` directly, legacy scaffold untouched) — not a scope question requiring the founder's input, so no AskUserQuestion was needed. An architecture was presented and approved with ten founder Decisions refining it before implementation. DECISIONS.md D-092 through D-102.

- [x] **Prisma schema**: `WebsiteDeployment` (immutable per-version rows, `@@unique([businessId, deploymentVersion])`, self-FKs `rollbackFromDeploymentId`/`retryOfDeploymentId`, every field the founder's Decision 8 + lifecycle sections named), `DeploymentHealthCheck` (one row per check run), `Domain` (mutable pointer, `currentDeploymentId` FK). Seven new enums (`DeploymentProviderName`/`DeploymentEnvironment`/`DeploymentStatus`/`HealthStatus`/`DomainType`/`DomainVerificationStatus`/`SslStatus`). New migration `20260806000000_m11_deployment` — DECISIONS.md D-092, D-093, D-094, D-095, D-096, D-099
- [x] `packages/shared-types` — 5 new schema files (`deployment-provider`/`website-deployment`/`deployment-health-check`/`domain`/`deployment-status`), each with its own `.test.ts`. `createWebsiteDeploymentSchema`/`createDomainSchema` deliberately have no `environment`/`provider` client-input fields — both are server-set, never client-chosen (founder's Decision 2: business layer never depends on a specific provider)
- [x] `apps/api/src/common/exceptions/app.exception.ts` — `WebsiteDeploymentNotFoundException`/`DomainNotFoundException`/`DeploymentValidationFailedException`/`DeploymentProviderUnavailableException`/`InvalidRollbackTargetException`/`DeploymentNotRetryableException`. `apps/api/src/common/rbac/permission.constants.ts` — four new permissions (`deployment:view`/`deployment:create`/`deployment:rollback`/`deployment:manage`); `admin`/`sales_manager` hold all four, `sales_executive` holds `deployment:view`/`deployment:create`, `developer`/`viewer` hold `deployment:view` only — DECISIONS.md D-098
- [x] **Deployment Provider** (`apps/api/src/deployment/provider/`) — `DeploymentProvider` interface + `DEPLOYMENT_PROVIDER` DI token, the structural mirror of `LocationProvider` (M5). `VercelAdapter` (real Vercel REST API calls, gated behind `VERCEL_API_TOKEN`/`VERCEL_TEAM_ID`) + `VercelProvider`, fully implemented. `CloudflarePagesProvider`/`NetlifyProvider`/`AwsAmplifyProvider`/`AzureStaticWebAppsProvider` each genuinely implement the interface (a one-line `useClass` swap away from being active) but throw "reserved for a future module" until a real adapter backs them — DECISIONS.md D-093
- [x] **Deployment Engine** (`apps/api/src/deployment/engine/`) — `DeploymentEngineService.requestDeployment()`/`retryDeployment()`/`deployGeneratedWebsite()` (the one orchestration method rollback/retry both funnel through), `DeploymentStatusService` (computed rollup, folded into the same module per the WebsiteStatusService/Pipeline-Engine precedent). Build Validation reads `WebsitePreviewService.getValidationReport()` (now exported from `WebsitePreviewModule`) and fails fast on any `error`-severity rule — never re-runs validators. A provider-call failure is caught and recorded as a `FAILED` row (`201`, not an HTTP error) — "Retry Available" is a normal outcome — DECISIONS.md D-094, D-097, D-102
- [x] **Health Check Engine** (`apps/api/src/deployment/health/`) — `HealthCheckEngineService.runCheck()`, four deterministic checks (deployment status / plain HTTP reachability / domain verification / SSL status), never calling `DeploymentProvider` (independent of providers, founder's Decision 5). Runs automatically after every successful deploy and on demand via `POST .../health-check`; never throws — a network blip can only produce `UNHEALTHY`, never retroactively fail an already-completed deployment — DECISIONS.md D-095
- [x] **Rollback Engine** (`apps/api/src/deployment/rollback/`) — `RollbackEngineService.rollbackTo()`, validates the target is `COMPLETED`+`HEALTHY` (`InvalidRollbackTargetException` otherwise), resolves its historical `GeneratedWebsite` snapshot, delegates to `DeploymentEngineService.deployGeneratedWebsite()` with `rollbackFromDeploymentId` set — never duplicates the deploy pipeline — DECISIONS.md D-094
- [x] **Domain Engine** (`apps/api/src/deployment/domain/`) — `DomainEngineService.create()`/`verify()`, metadata and provider-integration status only (founder's Decision 6) — no DNS automation, no SSL issuance, no registrar API calls anywhere. `attachDomain()` is best-effort: a provider without it, or a failed call, leaves the `Domain` row `PENDING` rather than failing registration — DECISIONS.md D-096
- [x] All five engines (`ProviderModule`/`EngineModule`/`HealthModule`/`RollbackModule`/`DomainModule`) registered in `apps/api/src/app.module.ts`; verified by running the full `AppModule` (e2e) test, which compiles and boots the entire DI graph with no route/token collisions against the pre-existing top-level `HealthModule` (the `/health` endpoint, unrelated to Deployment's Health Check Engine)
- [x] Structured logging: Deployment Requested/Started/Completed/Failed, Rollback Requested/Completed, Health Check Completed — each logged by the engine that owns the transition — DECISIONS.md D-100
- [x] Unit tests: 95 new in `apps/api` (34 provider, 17 engine + 6 status, 14 health, 7 rollback, 14 domain, across 18 spec files), 23 new in `packages/shared-types` (5 new `.test.ts` files) — 599 total in `apps/api` (up from 504), 239 in `packages/shared-types` (up from 216)
- [x] Integration tests: `apps/api/test/deployment.e2e-spec.ts` — 20 tests covering the full lifecycle wiring for all five engines, RBAC (deployment:view/create/rollback/manage each independently gated, including `deployment:view` reaching developer/viewer), publish-validation rejection, a real (fetch-mocked) `VercelProvider` deploy succeeding and failing, retry, rollback, manual health-check trigger, and domain registration — 203 e2e total (up from 183)
- [x] Full validation: monorepo build/lint/typecheck/test clean across every package and app (`packages/ui`, `apps/web`, `packages/logger` remain pre-existing zero-test packages, unrelated to this module)
- [ ] **Not done in this environment:** running the migration against a real Postgres, or a real deploy against Vercel's live API — same constraint as every prior module's external-dependency disclaimer; `VercelAdapter`'s tests mock `fetch` itself, never hitting the network
- [ ] **Known limitation, flagged not hidden:** `averageSalesCycleDays`-style approximations aren't a concern here, but `attachDomain()`'s SSL tracking is a simplification — `sslStatus` flips to `ACTIVE` alongside `verificationStatus` on verification rather than polling a separate "check SSL" provider call, since Vercel auto-provisions SSL the moment a domain verifies and Decision 6 rules out building SSL-issuance logic here regardless
- [ ] **Deliberately out of scope, per instruction:** website generation or modification, DNS automation, SSL certificate issuance, registrar APIs, GitHub/GitLab integration, CI/CD pipelines, preview/blue-green/canary/zero-downtime deployments — this module only deploys, manages, monitors, and versions deployments
- **Status: Module M11 implementation complete, pending your approval.**

## M12 — Analytics & Reporting

Founder first sent a diagram and a capability-recommendation list, then a complete formal module brief (objective — collect/aggregate/analyze/report platform data, must never modify business data or generate AI content, read-only; a research mandate to review every completed module and reuse existing infrastructure; sixteen named Analytics Scope domains; fifteen named reports; an Analytics Provider abstraction naming six providers; five named independent engines [Analytics, Reporting, Dashboard, Aggregation, Export]; daily/weekly/monthly/yearly/custom aggregation; CSV export real, PDF/Excel reserved; eight dashboard widgets; four dedicated permissions; explicit logging requirements; explicit performance constraints; Future Compatibility reservations; explicit constraints). A research pass across M1-M11 identified `CostService`/`ReportingService`(M10)/`AuditLogService`/`BusinessAnalysis`/the Deployment services as required-reuse infrastructure before any architecture was proposed. An architecture was presented — distinguishing internal Reporting (reads our own Postgres, no vendor) from the external `AnalyticsProvider` abstraction (vendor-agnostic event emission, a separate concern) — and approved with eleven founder Decisions refining it before implementation. DECISIONS.md D-103 through D-113.

- [x] **Prisma schema**: `AnalyticsEvent` (the only table this module owns — `eventType`/`entityType`/`entityId`/`actorId`/`provider`/`metadata`/`occurredAt`, populated exclusively by `SelfHostedAnalyticsProvider.track()`, never read back by any report this phase) plus `AnalyticsProviderName` enum. New migration `20260807000000_m12_analytics` — DECISIONS.md D-106
- [x] `packages/shared-types` — 6 new schema files (`analytics-provider`/`analytics-event`/`analytics-aggregation`/`analytics-report`/`analytics-dashboard`/`analytics-export`), each with its own `.test.ts`. `analyticsDashboardQuerySchema` deliberately named (not `dashboardQuerySchema`) to avoid colliding with M10's own `dashboard-stats.ts` export
- [x] `apps/api/src/common/exceptions/app.exception.ts` — `ExportFormatNotImplementedException`/`InvalidAggregationRangeException`. `apps/api/src/common/rbac/permission.constants.ts` — four new permissions (`analytics:view`/`analytics:report`/`analytics:export`/`analytics:manage`); `admin`/`sales_manager` hold all four, `developer`/`viewer` hold `analytics:view` only, `sales_executive` holds none — DECISIONS.md D-110
- [x] **Analytics Provider** (`apps/api/src/analytics/provider/`) — `AnalyticsProvider` interface + `ANALYTICS_PROVIDER` DI token, the structural mirror of M11's `DeploymentProvider`. `SelfHostedAnalyticsProvider`, fully implemented, writes to `AnalyticsEvent`. `PostHogProvider`/`GoogleAnalyticsProvider`/`MixpanelProvider`/`AzureApplicationInsightsProvider`/`DatadogProvider` each genuinely implement the interface but throw "reserved for a future module" until backed by a real adapter — DECISIONS.md D-105
- [x] **Aggregation Engine** (`apps/api/src/analytics/aggregation/`) — `AggregationEngineService.resolveRange()`/`bucketize()`, hand-rolled ISO Monday-start day/week/month/year bucket math (`bucket-math.ts`, no date library exists anywhere in this monorepo). Every trend-shaped Analytics Engine method and the Dashboard Engine both consume this same engine — DECISIONS.md D-107
- [x] **Analytics Engine** (`apps/api/src/analytics/engine/`) — `AnalyticsEngineService`, fifteen methods mapped to the founder's sixteen named domains, each a direct read of the M1-M11 table that already owns that fact. `getSalesFacts()` is a direct passthrough to M10's own `ReportingService.getDashboardStats()` (now exported for this purpose), never re-derived — DECISIONS.md D-104
- [x] **Reporting Engine** (`apps/api/src/analytics/reporting/`) — `ReportingEngineService.generateReport()`, `GET /analytics/reports/:type`, all fifteen founder-named reports wrapped in a standard envelope. `executive_dashboard` calls `DashboardEngineService.composeWidgets()` directly — the same composition the live dashboard uses, not a second implementation — DECISIONS.md D-108
- [x] **Dashboard Engine** (`apps/api/src/analytics/dashboard/`) — `DashboardEngineService.getDashboard()`/`composeWidgets()`, `GET /analytics/dashboard`, all eight founder-named widgets composed in parallel from existing Analytics Engine facts, never querying Prisma directly — DECISIONS.md D-108
- [x] **Export Engine** (`apps/api/src/analytics/export/`) — `ExportEngineService.exportReport()`, `GET /analytics/reports/:type/export`, CSV genuinely implemented (`csv-formatter.ts`, hand-rolled RFC-4180-ish escaping) via the exact same `ReportingEngineService.generateReport()` call the JSON endpoint uses, capped at 1000 rows. `pdf`/`excel` accepted by the schema but rejected with `EXPORT_FORMAT_NOT_IMPLEMENTED` (501) at the service layer before any report is computed — DECISIONS.md D-109
- [x] All three controller-bearing modules (`DashboardModule`, `ReportingModule` aliased `AnalyticsReportingModule`, `ExportModule`) registered in `apps/api/src/app.module.ts`; the aliasing resolves a real class-name collision with M10's own `crm/reporting/reporting.module.ts` `ReportingModule`. Verified by running the full `AppModule` (e2e) test, which compiles and boots the entire DI graph with no route/token collisions
- [x] Self-caught architectural gap fixed before this module was considered complete: the `AnalyticsProvider` abstraction was initially built but never actually called by any of the other four engines — fixed by wiring `ANALYTICS_PROVIDER` into `DashboardEngineService`/`ReportingEngineService`/`ExportEngineService` via a shared best-effort `trackBestEffort()` pattern (mirroring `AuditLogService.record()`, D-028) — DECISIONS.md D-105, D-111
- [x] Structured logging + best-effort provider tracking: Dashboard Viewed, Report Generated, Export Started, Export Completed (tracked and logged); Aggregation Completed (logged only — a high-frequency internal step, not a discrete user action) — DECISIONS.md D-111
- [x] Unit tests: 103 new in `apps/api` (19 provider, 24 aggregation, 20 engine, 20+ reporting, 12+ dashboard, 8+ export, across 12+ spec files), 35 new in `packages/shared-types` (6 new `.test.ts` files) — 702 total in `apps/api` (up from 599), 274 in `packages/shared-types` (up from 239)
- [x] Integration tests: `apps/api/test/analytics.e2e-spec.ts` — 20 tests covering all three controllers' guard/permission/validation-pipe wiring, RBAC across all four `analytics:*` permissions (including the `analytics:view`-only vs `analytics:report`/`analytics:export` split), the `executive_dashboard` report sharing the dashboard's own composition, cursor-paginated audit reports, custom-range validation, CSV export headers/content, and PDF/Excel rejection — 223 e2e total (up from 203)
- [x] Full validation: monorepo build/lint/typecheck/test clean across every package and app (`packages/ui`, `apps/web`, `packages/logger` remain pre-existing zero-test packages, unrelated to this module)
- [ ] **Not done in this environment:** running the migration against a real Postgres, or wiring a real vendor analytics platform — same constraint as every prior module's external-dependency disclaimer; `SelfHostedAnalyticsProvider` is the only provider ever actually exercised
- [ ] **Known limitation, flagged not hidden:** no caching is wired for any report/dashboard/export this phase — every request computes fresh from Postgres, the same deliberate "no caching yet, cache-ready shape" stance M10's own Reporting Engine (D-089) took
- [ ] **Deliberately out of scope, per instruction:** PDF/Excel generation, Scheduled Reports, Email Reports, Webhooks, KPI Alerts, Predictive Analytics, Machine Learning Insights, any mutation of another module's data or AI content generation — this module only reads, aggregates, and reports
- **Status: Module M12 approved.**

## v1.0 — Final Architecture Review & Release Documentation

Founder approved Module M12 ("the implementation satisfies the approved architecture and completes the planned v1.0 platform") and requested a documentation-only finalization pass before any Version 2.0 work: a final architecture review (M1-M12), module-boundary verification, an architecture consistency review, an API inventory, a database schema inventory, a permission matrix, a package dependency graph, a production deployment checklist, a security review checklist, a performance optimization checklist, a technical debt register, and a Version 2.0 roadmap. Explicit constraints: documentation only, no new features, no refactoring of working modules, no modification of previous modules. DECISIONS.md D-114.

- [x] Gathered and independently re-verified every quantitative fact against current source (not recalled from prior module review reports) — 73 API endpoints across 30 controllers, 33 Prisma models (28 active + 5 legacy-unused from M2) / 32 enums, 27 permissions × 6 roles, the full 11-package dependency graph, every environment variable referenced by `ConfigService`/`process.env` across the codebase
- [x] Verified module boundaries and dependency directions by grep sweep: `leads/`→`crm/`→`deployment/`→`analytics/` one-way dependency chain holds with zero violations; each module writes only to the Prisma models it owns (`analytics/` writes only `analyticsEvent`, `deployment/` only its three tables, `crm/` only its six tables plus the one founder-mandated `LeadActivityService` reuse); `@riznexia/ai` imported only where a founder brief named an AI step; package dependency graph confirmed acyclic
- [x] Full architecture consistency review — confirmed every recurring pattern (provider abstraction, Prisma/API enum split, closed-enum discipline, `resource:verb` permissions, `AppException` hierarchy, best-effort side-effects, "read already-computed facts," computed-not-persisted rollups, immutable-vs-mutable entity distinction, hand-rolled small logic over new dependencies, naming-collision resolution) held consistently from its first module through M12, no inconsistency found
- [x] Generated `docs/35-v1.0-architecture-review.md` — one consolidated document, twelve sections, covering all twelve founder-requested deliverables
- [x] Zero source code created, modified, or deleted to produce this pass — verified by `git status` showing only documentation files changed
- [x] `docs/21-implementation-roadmap.md` remains untouched, consistent with every module's own treatment of that file throughout M1-M12
- **Status: v1.0 finalization documentation complete, pending your approval to begin Version 2.0.**

# Frontend Modules (F1-F15)

Frontend roadmap frozen per the founder's brief: F1 Foundation, F2 Dashboard, F3 Lead Discovery, F4 Lead Management, F5 Google Places, F6 AI Business Analysis, F7 Theme Engine, F8 Website Generator, F9 Website Preview, F10 Sales CRM, F11 Deployment, F12 Analytics, F13 User Management, F14 Settings, F15 Profile. Stack frozen: Next.js 15 (App Router) / React 19 / TypeScript, Tailwind v4 / shadcn/ui / Lucide / Framer Motion (minimal), TanStack Query / React Hook Form / Zod. Every module: architecture presented and approved before any code is written, backend (M1-M12) frozen — no backend module renamed/merged/split/reordered, no backend contract changed, frontend consumes existing APIs only.

## F1 — Foundation

Founder specified the complete application foundation every later frontend module builds on: folder structure, routing, auth flow, protected/public routes, layout architecture (sidebar/header), theme system, state management, API client, query client, error handling, loading strategy, toast system, form/validation strategy, component architecture, shared UI package usage, responsive breakpoints, dark mode, environment variables, build configuration. A complete architecture was presented (grounded in the actual frozen backend — Clerk auth wiring, the exact error envelope, the v1.0 review's 73-endpoint/27-permission inventory — and the pre-existing `docs/08`/`docs/17` design system, re-mapped onto the frozen F1-F15 structure) and approved before any code was written. DECISIONS.md D-115 through D-121.

- [x] **`packages/ui`** — shared component library populated for the first time (previously a placeholder): shadcn/ui primitives (Button, Input, Label, Avatar, DropdownMenu, Sheet, Skeleton, Separator, Tooltip) hand-built on Radix + CVA, the RHF/Zod `Form` integration surface, a `Toaster` wrapping `sonner` as the one seam every app calls through, and an `ErrorState` mirroring the backend's exact error envelope shape. Every Radix/RHF-wrapping component carries an explicit `'use client'` directive — a real cross-package React Server Components build failure found and fixed during implementation, not assumed upfront — DECISIONS.md D-119
- [x] **`apps/admin-web`** — new Next.js 15/React 19 app (`apps/web` explicitly left untouched, flagged for confirmation) — Tailwind v4 CSS-first theming (`docs/08`/`docs/17`'s color/type/spacing tokens as CSS custom properties + registered `--text-*` scale), shared `@riznexia/config-typescript`/`config-eslint` configs reused as-is — DECISIONS.md D-115
- [x] **Authentication** — `@clerk/nextjs`, `middleware.ts` (`clerkMiddleware`, coarse "is there a session" gate), `(auth)` route group wrapping Clerk's own hosted `<SignIn/>`/`<SignUp/>` (no custom credential form — the backend has none to submit to), `(dashboard)/layout.tsx` (authoritative second gate — `GET /me`, a 401 renders a dedicated `<NotProvisioned/>` state rather than redirecting into a sign-in loop) — DECISIONS.md D-115, D-118
- [x] **Layout** — `AppShell` (Sidebar + Header + capped-width main), `Sidebar`/`MobileSidebar` (persistent `lg`+, a Sheet drawer below it) both rendering the same `SidebarNav`, `Header` (mobile menu trigger, `Breadcrumb`, `GlobalSearch` navigating to `/leads?q=` — the backend's real query param, not `search`), `UserMenu` (profile link, `ThemeToggle`, sign-out), all nav items gated by a small admin-web-local role table (`src/lib/auth.ts`), deliberately not a copy of the backend's 27-permission enforcement matrix — DECISIONS.md D-116
- [x] **Theme system** — `next-themes`, default dark, both modes independently tuned per `docs/17` §16, persisted client-side only (localStorage/cookie) — a deliberate, flagged deviation from the pre-existing design doc's "sync via `team_member`" aspiration, forced by the frozen backend having no such column — DECISIONS.md D-117
- [x] **API client / Query client** — `apiClient` (`get`/`post`/`patch`/`delete`, one `fetch` wrapper), `ApiError` (carries the backend's exact `{code, message, details, status}`), dev-only Zod response-schema drift warnings (never a production hard-fail), `createQueryClient()` factory (never a module-level singleton — one instance per request via `useState`), 4xx never retried, network/5xx retried twice
- [x] **Error handling / Loading / Toast** — `app/error.tsx` (crash boundary, distinct from inline `<ErrorState/>`), `app/global-error.tsx` (root-layout crash, self-contained, no design-system dependency), `app/not-found.tsx`, `(dashboard)/loading.tsx` (skeletons, not spinners, per `docs/17` §18), `Toaster`/`toast` re-exported from `packages/ui`
- [x] **Form / validation strategy** — the `Form`/`FormField`/`FormItem`/`FormControl`/`FormMessage` RHF+Zod wiring is built and proven end-to-end by its own test (no real business form exists yet — no F2-F15 module owning one has landed)
- [x] **Route scaffold** — every top-level route (`/`, `/discovery`, `/leads`, `/crm`, `/analytics`, `/team`, `/settings`, `/profile`) ships as a real, navigable page rendering a shared `<FeaturePlaceholder/>` naming the future module that owns its real content — including `/` itself, since the Dashboard home screen is F2's content, not F1's — DECISIONS.md D-120
- [x] Shared `@riznexia/config-eslint/nextjs.js` extended (not the underlying named-exports rule) to recognize three more framework-mandated default-export files F1 introduced (`global-error.tsx`, `middleware.ts`, `vitest.config.*`) — additive tooling maintenance, verified to change no outcome for any non-frontend package — DECISIONS.md D-121
- [x] Unit tests: 12 new in `packages/ui` (Button, ErrorState, Form/RHF-Zod integration, Toaster — 4 spec files), 22 new in `apps/admin-web` `src/lib` (env, api-client, query-client, auth nav-visibility — 4 spec files)
- [x] Integration tests: 14 new in `apps/admin-web` (SidebarNav role-visibility + active-link + onNavigate, Breadcrumb pathname-matching, GlobalSearch navigation, NotProvisioned, app-level ErrorBoundary and NotFound — 6 spec files) — 36 `apps/admin-web` tests total, 48 F1 frontend tests total
- [x] Full validation: monorepo build/lint/typecheck/test clean across every package and app; a real `next build` (not just typecheck/lint/test) caught the RSC `'use client'` boundary bug (D-119) that the rest of the suite missed entirely
- [ ] **Not done in this environment:** no real Clerk application was exercised (build/tests use a dummy publishable/secret key pair) — same external-dependency constraint as every prior module
- [ ] **Known limitation, flagged not hidden:** Next.js Server Components (`(dashboard)/layout.tsx`, `middleware.ts`) aren't directly RTL-testable — their logic is verified by unit-testing `apiClient`'s error shape and by rendering `<NotProvisioned/>`/`<AppShell/>` standalone, not by a true integration test of the server-side request chain; a real end-to-end check would need a running dev server and a browser tool (e.g. Playwright), out of scope for this module's automated suite
- [ ] **Deliberately out of scope, per instruction:** no F2-F15 business screen, no business logic, no backend change — this module is the shell every later module fills in
- **Status: Module F1 approved.**

### F1 addendum — RBAC Alignment

Founder approved F1 and requested one small improvement before F2: replace the sidebar's local role-list nav mapping with a permission-based architecture compatible with the backend RBAC system — navigation visibility, route guards, and component rendering all driven by permission, never a hardcoded role check inside a UI component, no new API call, no backend change, theme storage/`apps/web` untouched. DECISIONS.md D-122.

- [x] `src/lib/permissions.ts` — mirrors the backend's `PERMISSIONS`/`ROLE_PERMISSIONS` grant table verbatim (27 permissions × 6 roles), presentation-only, same category as D-116's original mirror, now in the backend's own vocabulary
- [x] `src/lib/permissions-context.tsx` — `PermissionsProvider` (derives the permission set once from the session's `role`, already available from the existing `GET /me` call — no new fetch), `usePermissions()`/`useHasPermission()`/`useHasAnyPermission()`
- [x] `src/lib/route-guard.ts` — `assertPermission()`/`hasPermission()`/`hasAnyPermission()` for Server Components (`notFound()` on failure) — unused by any route today, ready for the first future module that needs one
- [x] `src/components/auth/permission-gate.tsx` — `<PermissionGate permission=".."/anyOf={[...]}>` for permission-based conditional rendering in any component
- [x] `auth.ts`'s `NAV_ITEMS`: `allowedRoles: TeamRole[]` → `requiredPermission: Permission | null`, each value verified to reproduce F1's original visibility exactly, not re-decided
- [x] `Sidebar`/`MobileSidebar`/`SidebarNav`/`Header` no longer take a `role` prop or import `TeamRole` — `AppShell` wraps its tree in `PermissionsProvider` once; no component outside `permissions.ts` branches on role anywhere in `apps/admin-web`
- [x] Tests updated/added: `auth.test.ts` rewritten for the permission-driven table, `permissions.test.ts` (mirrors-the-backend-matrix drift check), `permissions-context.test.tsx`, `route-guard.test.ts`, `permission-gate.test.tsx`, `sidebar-nav.test.tsx` rewritten to render inside `PermissionsProvider` — 56 `apps/admin-web` tests total (up from 36), 68 F1-lineage frontend tests total
- [x] Full validation: monorepo build/lint/typecheck/test clean; a real `next build` re-run clean
- **Status: RBAC Alignment complete, proceeding to Module F2 per your instruction.**

## F2 — Dashboard

Founder approved the F2 architecture (grounded in the two real backend dashboard endpoints — `GET /analytics/dashboard` M12, `GET /crm/dashboard` M10 — rather than the pre-M10 `docs/17` wireframe) and requested two improvements folded in before implementation: a Dashboard Widget Registry (Dashboard → Widget Registry → Individual Widgets, extensible for future widgets) and configurable auto-refresh (Manual/30s/1m/5m via TanStack Query `refetchInterval`, no new backend endpoint). The architecture proposal's own two open questions — what `sales_executive` sees, and whether a charting library is needed — were resolved per DECISIONS.md D-125/D-127. DECISIONS.md D-123 through D-127.

- [x] **Widget Registry** (`src/features/dashboard/widgets/`) — `DashboardWidgetDefinition` interface + `DASHBOARD_WIDGETS` array, mirroring the backend's own `VALIDATOR_REGISTRY` idiom (M9). `WidgetRegistry` is the only thing `DashboardHome` renders for the M12 widget grid; it has no knowledge of `LeadsWidget`/`SalesWidget`/etc. Eight self-contained widgets (Leads/Sales/AI Usage/Costs/Deployments/Website Status/Conversion/System Health), each calling the shared `useAnalyticsDashboard()` query itself (deduped by TanStack Query — 8 mounted components, 1 network request) — DECISIONS.md D-123
- [x] **Configurable auto-refresh** (`src/features/dashboard/refresh-interval.tsx`) — `RefreshIntervalProvider` (Manual/30s/1m/5m), `useQueryRefetchInterval()` mapping directly onto `useQuery`'s `refetchInterval` option, no bespoke polling code. One shared setting governs every dashboard query; `DashboardToolbar`'s manual "Refresh now" button invalidates all three dashboard query keys regardless of which sections are mounted for the current role — DECISIONS.md D-124
- [x] **Data hooks** (`src/features/dashboard/api/`) — `useAnalyticsDashboard(period)`, `useCrmDashboard()` (no period param — the backend endpoint has none; deliberately not invented), `useMyTasks()` (the `sales_executive` fallback's data source). All three existing endpoints only — no new endpoint, no new API call beyond what the approved architecture named
- [x] **`sales_executive` fallback** — `DashboardHome` branches on `useHasPermission('analytics:view')`/`useHasPermission('crm:report')`; a role holding neither renders `<MyWorkPanel/>` (`GET /crm/tasks?assignedToId=<self>&limit=5`, an endpoint they already hold `crm:view` for) instead of an empty page — a short worklist, deliberately not a KPI dashboard, since neither `/leads` nor `/crm/tasks` exposes a total count cheaply enough for one — DECISIONS.md D-125
- [x] **`CurrentUserProvider`** (`src/lib/current-user-context.tsx`) — the session's `TeamMember` (already fetched via `GET /me`) made available by context alongside `PermissionsProvider`, for `MyWorkPanel`'s `assignedToId` filter — no second `/me` call — DECISIONS.md D-126
- [x] **No charting library added** — pipeline-by-stage (`CrmPipelineSection`) and the costs/system-health widgets' progress bars are hand-rolled proportional `<div>`s, matching this codebase's established precedent for small, well-defined visual logic; every M12 widget shape is a single number/percentage/count, none needed a real chart — DECISIONS.md D-127 (a deliberate reversal of the architecture proposal's own tentative Recharts default, made once the real widget shapes were inspected)
- [x] Period selector (`daily`/`weekly`/`monthly`/`yearly`) lives in the URL (`?period=`), bookmarkable; `custom` is excluded from the control since no date-range picker exists yet and selecting it without `from`/`to` would 400
- [x] Route: `app/(dashboard)/page.tsx` — F1's `<FeaturePlaceholder moduleId="F2">` replaced with the real `<DashboardHome/>`
- [x] Unit tests: 20 new (`format.test.ts`, `query-string.test.ts`, `current-user-context.test.tsx`, `refresh-interval.test.tsx`, `use-dashboard-period.test.tsx`, `widget-registry.structure.test.ts`)
- [x] Integration tests: 15 new (`dashboard-home.test.tsx` — full role-based composition: admin sees widgets+CRM section, developer sees widgets only, `sales_executive` sees My Work only; `period-select.test.tsx`, `refresh-interval-select.test.tsx`, `dashboard-toolbar.test.tsx`) — 91 `apps/admin-web` tests total (up from 56)
- [x] Full validation: monorepo build/lint/typecheck/test clean; a real `next build` re-run clean (no RSC boundary issues this time — every new client component correctly marked from the start)
- [ ] **Not done in this environment:** no real Clerk application or backend was exercised — build/tests use dummy Clerk keys and a mocked `fetch` boundary, same constraint as F1
- [ ] **Known limitation, flagged not hidden:** the period selector only drives the M12 analytics widgets — `GET /crm/dashboard` has no period/date-range param of its own, so the CRM pipeline section always shows all-time data regardless of the toolbar's period selection; not a bug, the backend contract simply has no date filter concept for this endpoint beyond an unexposed `fromDate`/`toDate`
- [ ] **Known limitation, flagged not hidden:** `custom` period has no date-range picker UI yet — reserved in the selector's underlying type but not offered as a clickable option
- [ ] **Deliberately out of scope, per instruction:** no F3-F15 business screen, no backend change, no additional API call beyond the three endpoints the approved architecture named
- **Status: Module F2 implementation complete, pending your approval.**

## F3 improvement — Shared Data Table

Founder approved F2 and, before authorizing F3, requested one improvement: a reusable, fully generic Data Table in `packages/ui` — server-side pagination, sorting, global search, column filters, column visibility, row selection, bulk actions, empty/loading/error states, responsive layout — with an explicit constraint that the implementation be generic, with no business- or Lead-specific logic, for every future module to reuse. DECISIONS.md D-128, D-129.

- [x] **`DataTable<TData>`** (`packages/ui/src/components/data-table/`) — built on `@tanstack/react-table`, the one deliberate exception to this codebase's own "hand-roll small logic" precedent, since sorting/filtering/pagination/selection interacting correctly together is substantial, well-trodden functionality — DECISIONS.md D-128
- [x] **Server-side pagination** — `DataTablePaginationConfig`'s `'server'` mode is Prev/Next against `hasNextPage`/`hasPreviousPage`/`onNextPage`/`onPreviousPage`, modeled on this system's actual cursor-based backend contracts (no endpoint anywhere supports page-N jump) — DECISIONS.md D-129
- [x] **Client-side pagination** — `'client'` mode (page-size select, "Page X of Y", computed by TanStack's own row model) for callers with the full dataset already in memory
- [x] **Sorting** — dual `'client'`/`'server'` modes (`manualSorting` toggled accordingly); `DataTableColumnHeader` is a consumer-facing sort-toggle button, works identically in both modes
- [x] **Global search, column filters, column visibility** — `DataTableToolbar`; per-column filter UI (`text` input or `select`) driven entirely by a declarative `columnDef.meta: {label, filterVariant, filterOptions}` convention — the table itself has zero knowledge of which columns exist or what they mean
- [x] **Row selection + bulk actions** — auto-injected synthetic `__select__` checkbox column when `enableRowSelection` is set; toolbar swaps to a bulk-actions bar once any row is selected
- [x] **Empty / loading / error states** — skeleton rows while loading, `ErrorState` with retry, a configurable empty-state message
- [x] **Responsive layout** — `Table` wraps in `overflow-x-auto`
- [x] Proven generic by its own test suite using a made-up `Widget` row type (deliberately not `Lead`/`DiscoveryJob`) — 13 tests covering every feature above in isolation
- [x] `StatusBadge` (`packages/ui/src/components/status-badge.tsx`) — pre-announced in F1's own `index.ts` comment as landing with its first consuming screen module; 5 semantic tones (`neutral|info|success|warning|danger`) + label only, never a domain status string — 3 tests
- [x] Unit tests: 16 new `packages/ui` tests (13 DataTable, 3 StatusBadge) — `packages/ui` 28 tests total
- [x] Full validation: `packages/ui` typecheck/lint/test clean
- **Status: Shared Data Table complete, proceeding to Module F3 per your instruction.**

## F3 — Lead Discovery

Founder approved the F3 architecture — grounded in the real Discovery backend (`GET/POST /discovery-jobs`, `GET /discovery-jobs/:id`), reflecting its actual behavior rather than any invented capability. Approved decisions: client-side pagination over the existing top-50 `GET /discovery-jobs` response (no pagination params exist on that endpoint), Job Details showing only the fields the backend returns, status-based (not percentage) progress, Import Summary as a bare `resultsCount`, search filters limited to City/Categories(max 5)/Radius, permission model `discovery:read`/`discovery:run`. DECISIONS.md D-130 through D-133.

- [x] **`status.ts`** — `DISCOVERY_STATUS_PRESENTATION` (all 4 `DiscoveryJobStatus` values → `StatusBadge` variant + label), `DISCOVERY_STATUS_OPTIONS`, `isTerminalDiscoveryStatus()` — the only place Discovery's domain status knowledge lives, kept out of the generic `StatusBadge`/`DataTable`
- [x] **Data hooks** (`src/features/discovery/api/`) — `useDiscoveryJobs()` (`GET /discovery-jobs`, no query params — matches the real signature exactly), `useDiscoveryJob(jobId)` (`GET /discovery-jobs/:id`, polls every 3s via a function-form `refetchInterval` gated on `isTerminalDiscoveryStatus`, stops automatically on `completed`/`failed`), `useCreateDiscoveryJob()` (`POST /discovery-jobs`, invalidates the list query on success)
- [x] **Search Filters / New Search form** — `DiscoverySearchForm` (RHF + `createDiscoveryJobSchema`), `CategoryInput` (chip UI, Enter/comma to add, max 5, duplicate/blank rejection) — City, Categories, Radius (km) only, the exact fields the backend's create-job schema accepts, gated behind `<PermissionGate permission="discovery:run">`
- [x] **Discovery History** — `DiscoveryHistoryTable`, `<DataTable/>` wired with client pagination (D-130), client sorting, global search, column filters (city/category text, status select), column visibility; status column filter deliberately `select` (closed 4-value enum) while category is deliberately `text` (no closed taxonomy exists server-side) — DECISIONS.md D-132
- [x] **Discovery Progress** — `DiscoveryProgress`, a 3-step `queued→running→completed` track plus a structurally separate failed-state branch, status-based per the approved architecture, never a percentage — DECISIONS.md D-131
- [x] **Import Summary** — `DiscoveryImportSummary`, renders only for `status === 'completed'`, showing `resultsCount` and a one-line summary sentence built from fields the response already has — no business list, no drill-in — DECISIONS.md D-133
- [x] **Job Details page** — `app/(dashboard)/discovery/[jobId]/page.tsx` (this app's first dynamic route, Next 15 async `params`), `DiscoveryJobDetail` composing `DiscoveryProgress` + `DiscoveryImportSummary`, showing only `{city, category, status, resultsCount}` — no `createdAt`/`errorMessage` fields, since the backend doesn't return them
- [x] **Route**: `app/(dashboard)/discovery/page.tsx` — F1's `<FeaturePlaceholder moduleId="F3 / F5">` replaced with the real `<DiscoveryPage/>`
- [x] Unit tests: 14 new (`status.test.ts`, `use-discovery-job.test.tsx` — fake-timers polling proof, `category-input.test.tsx`)
- [x] Integration tests: 18 new (`discovery-search-form.test.tsx`, `discovery-history-table.test.tsx`, `discovery-page.test.tsx`, `discovery-progress.test.tsx`, `discovery-import-summary.test.tsx`, `discovery-job-detail.test.tsx`) — 32 `src/features/discovery` tests total, 123 `apps/admin-web` tests total (up from 91)
- [x] Full validation: monorepo build/lint/typecheck/test clean for `packages/ui` and `apps/admin-web`; a real `next build` re-run clean, `/discovery` (5.41 kB) and `/discovery/[jobId]` (1.54 kB) both build with no RSC boundary issue
- [ ] **Not built, per explicit scope:** Lead Management, CRM, Google Places — F3 built only Discovery Jobs/History/Progress/Search Filters/Job Details/Pagination/Import Summary
- [ ] **Not done in this environment:** no real Clerk application or backend was exercised — build/tests use dummy Clerk keys and a mocked `fetch` boundary, same constraint as F1/F2
- [ ] **Deliberately out of scope, per instruction:** no backend API modified, no previous frontend module modified beyond the one placeholder swap in `discovery/page.tsx`
- **Status: Module F3 implementation complete, pending your approval.**

## F4 — Lead Management

Founder specified the full Lead Management frontend up front: Lead List (search, sorting, client/server pagination "according to backend", status/tags/assigned-user filters, bulk selection, empty/loading/error states), Lead Details (every backend-returned field — Business/Contact/Google Places Information, Status, Tags, Assigned User, Notes, Activity Timeline — "do not invent fields"), Create/Edit (RHF + Zod, reusing F1 form components, backend validation errors shown exactly), append-only Notes, read-only newest-first Activity Timeline, `leads:read`/`leads:write`/`leads:delete` permission gating, reuse of DataTable/StatusBadge/Skeletons/ErrorState/PermissionGate. Backend research surfaced four real gaps between that spec and what `apps/api/src/leads/` actually returns (no business-search endpoint, no Contact/Google Places fields on the Lead response, no team-member list endpoint, no bulk endpoint) — each was presented to the founder as an explicit choice and approved before implementation. DECISIONS.md D-134 through D-140.

- [x] **`packages/ui`: `AlertDialog` + `Textarea`** — two new generic primitives, F4's first real need for a destructive-confirmation modal and a multi-line input; neither has any lead-specific knowledge — DECISIONS.md D-139
- [x] **Lead List** (`src/features/leads/components/lead-list-table.tsx`) — server-mode `<DataTable/>` (cursor pagination via `useLeads`, matching `GET /leads`'s real cursor/`nextCursor` contract — DECISIONS.md D-140), server-mode sorting (whitelisted `LEAD_SORT_FIELDS` only: createdAt/updatedAt/pipelineStage/businessName), a bespoke filter bar (`lead-list-filters.tsx`) driving the real `q`/`stage`/`tag`/`assignedTo` query params directly rather than DataTable's own page-local filter UI — DECISIONS.md D-138
- [x] **Filters** — Status (select, closed 6-value `PipelineStage` enum), Tags (free-text, single tag — the backend supports exactly one `tag=` param, no multi-tag AND/OR), Assigned User ("Assigned to me" only, no "Unassigned" option — no team-member list endpoint exists — DECISIONS.md D-136), Search (`q`, debounced, min 2 chars matching `listLeadsQuerySchema`)
- [x] **Bulk selection** — real bulk delete (`leads:delete`) and bulk "add tag" (`leads:write`) actions, each run as N sequential `Promise.allSettled` requests against the existing single-lead endpoints (no atomic bulk endpoint exists), confirmation dialogs stating plainly "N individual requests, not one atomic operation", partial failure reported by count — DECISIONS.md D-137
- [x] Empty / loading / error states — reused directly from the shared DataTable (skeleton rows, `ErrorState` + retry, configurable empty message)
- [x] **Lead Details** (`components/lead-detail-page.tsx` + `lead-detail/*`) — Business Information (businessName/category/city/address/websiteStatus, exactly what `GET /leads/:id` returns), Status/Tags/Assigned User (one combined section), Contact Information / Google Places Information rendered with an honest "Not available — not returned by the current API" placeholder rather than omitted or fabricated — DECISIONS.md D-135
- [x] **Notes** (`lead-detail/lead-notes-panel.tsx`) — append-only (`POST /leads/:id/notes`, `GET /leads/:id/notes`), no edit/delete affordance anywhere (matches `LeadNotesService`'s own "deliberately no update or delete path"), add-note form gated on `leads:write`
- [x] **Activity Timeline** (`lead-detail/lead-activity-timeline.tsx`) — `GET /leads/:id/activity`, read-only (no write endpoint exists), newest-first per the backend's own ordering, all 14 `LeadActivityType` values labeled (`lead-activity-presentation.ts`)
- [x] **Create Lead** (`components/create-lead-form.tsx`) — raw Business ID (UUID) input, since no business-search/list endpoint exists anywhere in the backend — a known, flagged limitation, not an invented capability — DECISIONS.md D-134
- [x] **Edit Lead** (`components/edit-lead-form.tsx`) — `PATCH /leads/:id`, always sends `assignedTo` explicitly (a UUID or `null`), matching the backend's own omitted-vs-explicit-null distinction
- [x] Both forms use RHF + `zodResolver`, reuse F1's `Form`/`FormField`/`FormItem`/`FormControl`/`FormMessage`/`Input`/`Button`, and show the backend's `ApiError.message` verbatim on failure (the same pattern F3's `DiscoverySearchForm` established)
- [x] **Permissions** — `leads:write` gates the New/Edit forms and their routes (first real consumer of `route-guard.ts`'s `assertPermission`, built ahead of its own first user in RBAC Alignment), `leads:delete` gates every delete affordance, both hidden (not disabled) when the permission is missing; `leads:read` needs no gate since all 6 roles hold it
- [x] Routes: `app/(dashboard)/leads/page.tsx` (F1 placeholder replaced), `leads/new/page.tsx`, `leads/[leadId]/page.tsx` (this app's second dynamic route), `leads/[leadId]/edit/page.tsx`
- [x] Unit tests: 15 new (`lead-stage.test.ts`, `lead-activity-presentation.test.ts`, `tag-input.test.tsx`, `use-bulk-delete-leads.test.tsx`, plus 2 new `packages/ui` tests — `alert-dialog.test.tsx`, `textarea.test.tsx`)
- [x] Integration tests: 25 new (`lead-list-table.test.tsx`, `lead-list-page.test.tsx`, `lead-detail-page.test.tsx`, `create-lead-form.test.tsx`, `edit-lead-form.test.tsx`) — 40 `src/features/leads` tests total, 163 `apps/admin-web` tests total (up from 123); `packages/ui` 33 tests total (up from 28)
- [x] Full validation: monorepo build/lint/typecheck/test clean for `packages/ui` and `apps/admin-web`; a real `next build` re-run clean — `/leads` (3.92 kB), `/leads/[leadId]` (4.18 kB), `/leads/[leadId]/edit` (1.68 kB), `/leads/new` (1.51 kB), no RSC boundary issues
- [ ] **Not built, per explicit scope:** no backend API modified, no previous frontend module modified beyond the one placeholder swap in `leads/page.tsx`
- [ ] **Known limitation, flagged not hidden:** Create Lead's Business ID field has no in-app way to discover a valid id — usable only by someone who already has one (DECISIONS.md D-134)
- [ ] **Known limitation, flagged not hidden:** Contact Information and Google Places Information render as honest placeholders, not real data — no backend endpoint returns those fields for a lead (DECISIONS.md D-135)
- [ ] **Known limitation, flagged not hidden:** "Assigned User" can only ever show "You" or a raw UUID for other users — no team-member list/name-lookup endpoint exists (DECISIONS.md D-136)
- **Status: Module F4 implementation complete, pending your approval.**

## F5 — Google Places Synchronization

Founder specified Place Sync Dashboard/Sync Job History/Sync Job Details up front, with several fields explicitly hedged ("Resume existing synchronization (if supported)", "Current Page Token (if returned)", "Error Message (if available)") — an acknowledgment that some named features might not exist on the backend. Research against `apps/api/src/place-sync/` confirmed several genuinely don't (no resume/pause capability, no page-token field), and surfaced two further gaps the brief didn't hedge (no `updatedAt` field, no reserved nav slot — F1's own `/discovery` placeholder was labeled "F3 / F5" but F3 took the base route). Each real gap was presented to the founder as an explicit choice and approved before implementation. DECISIONS.md D-141 through D-148.

- [x] **Routing** (`app/(dashboard)/discovery/sync/*`) — three new routes nested under Discovery's own area, plus one new `NAV_ITEMS` entry in `src/lib/auth.ts` ("Place Sync") — the founder-approved resolution to F1's own "F3 / F5" shared-placeholder anticipation; the one pre-existing exact-list test assertion this touched (`auth.test.ts`) was updated to match — DECISIONS.md D-141
- [x] **`status.ts`** — `PLACE_SYNC_STATUS_PRESENTATION` (all 5 `PlaceSyncJobStatus` values → `StatusBadge` variant + label — `queued/running/completed/failed/partial`, verified there is no `CANCELLED` value for this job type), `PLACE_SYNC_STATUS_OPTIONS`, `isTerminalPlaceSyncStatus()` (three real terminal outcomes — `completed`/`failed`/`partial` — not two)
- [x] **Data hooks** (`src/features/place-sync/api/`) — `usePlaceSyncJobs()` (`GET /place-sync-jobs`, no query params, fixed top-50 — matches the real signature exactly), `usePlaceSyncJob(jobId)` (`GET /place-sync-jobs/:id`, polls every 3s while non-terminal, stops automatically on any of the three terminal statuses), `useCreatePlaceSyncJob()` (`POST /place-sync-jobs`, invalidates the list on success)
- [x] **Place Sync Dashboard** (`place-sync-dashboard-page.tsx`) — "Start new synchronization" (`PlaceSyncSearchForm`, gated on `discovery:run`), "Progress summary"/"Latest synchronization status" (`PlaceSyncLatestStatus` — the first item of the already-`createdAt desc`-ordered list, manual refresh); "Resume existing synchronization" deliberately not built — no backend concept exists — DECISIONS.md D-142
- [x] **Search Parameters** — city/category/keyword/latitude/longitude/radiusMeters, per-field constraints matching `createPlaceSyncJobSchema` exactly; the backend's own cross-field rule (city or lat/long required; city-only needs category or keyword) is deliberately not duplicated client-side — a violation is caught by the real endpoint, its exact message shown via toast — DECISIONS.md D-148
- [x] **Sync Job History** (`place-sync-history-table.tsx` + `place-sync-history-columns.tsx`) — `<DataTable/>` in full `client` mode (pagination/sorting/global search/column filters/column visibility) — `GET /place-sync-jobs` has zero pagination/sort/filter params, the identical fixed-top-50 shape as Discovery History (D-130) — DECISIONS.md D-146. Columns: City, Category, Status (badge + select filter), Started At (`startedAt`), Completed At (`finishedAt`), Imported (`businessesCreated`), Updated (`businessesUpdated`), Failed (`businessesFailed`)
- [x] **Sync Job Details** (`place-sync-job-detail.tsx`) — Job Status, Search Parameters, Processed Count (computed sum of the three real counters — DECISIONS.md D-144), Imported/Updated/Failed Counts, Error Message (shown only when the backend actually returns one), Timeline (Started At/Completed At/Duration), Last Updated (derived from the most recent real timestamp — DECISIONS.md D-143); no "Current Page Token" section at all — the field is never persisted or returned by any endpoint, a bare omission rather than a placeholder, since the founder's own brief hedged this one — DECISIONS.md D-145
- [x] **`PlaceSyncProgress`** — status-based, never a percentage (no live created/updated/failed tally exists mid-run); three distinct terminal branches — the normal 3-step track, a `failed` branch, and a separate `partial` branch (a real third outcome Discovery never had: the run finished but some businesses failed individually) — DECISIONS.md D-147
- [x] **Permissions** — `discovery:run` gates the New Synchronization form only; `discovery:read` needs no gate (universal across all 6 roles) — no new permission introduced, reusing exactly the two named in the brief
- [x] **UI reuse** — DataTable, StatusBadge, PermissionGate, ErrorState, Skeletons, AppShell all reused directly; zero duplicated components
- [x] Unit tests: 10 new (`status.test.ts`, `use-place-sync-job.test.tsx` — fake-timers polling proof including the `partial` terminal case, `place-sync-progress.test.tsx` — all 4 branches)
- [x] Integration tests: 21 new (`place-sync-search-form.test.tsx`, `place-sync-history-table.test.tsx`, `place-sync-latest-status.test.tsx`, `place-sync-job-detail.test.tsx`, `place-sync-dashboard-page.test.tsx`) — 31 `src/features/place-sync` tests total, 194 `apps/admin-web` tests total (up from 163)
- [x] Full validation: monorepo build/lint/typecheck/test clean; a real `next build` re-run clean — `/discovery/sync` (5.75 kB), `/discovery/sync/history` (1.83 kB), `/discovery/sync/[jobId]` (2.07 kB), no RSC boundary issues
- [ ] **Not built, per explicit scope:** no backend API modified; no previous frontend module modified beyond the one additive `NAV_ITEMS` entry (and its one mechanically-updated test assertion) the founder explicitly approved
- [ ] **Known limitation, flagged not hidden:** "Resume existing synchronization" does not exist — the backend has no resume/pause concept for Place Sync jobs at all (DECISIONS.md D-142)
- [ ] **Known limitation, flagged not hidden:** "Last Updated" can only ever be a derived value (`finishedAt`/`startedAt`/"No activity yet") — no real `updatedAt` field exists (DECISIONS.md D-143)
- [ ] **Not done in this environment:** no real Clerk application or backend was exercised — build/tests use dummy Clerk keys and a mocked `fetch` boundary, same constraint as F1-F4
- **Status: Module F5 implementation complete, pending your approval.**

## F6 — AI Business Analyzer

Founder specified Business Analysis Dashboard/Analysis History/Analysis Details up front, with several fields explicitly hedged ("Re-run Analysis (only when backend allows)", "Display cache status if returned by backend") and an explicit conditional rule for History ("If backend only returns latest analysis, display latest only. Do not fabricate history."). Research against `apps/api/src/business-analysis/` confirmed the hedges were warranted and surfaced one further gap the module's own blanket rule already resolves without a founder round-trip: `promptHash` is on the Prisma model but never serialized into the API response. Routing followed F5's now-settled "no reserved nav slot" precedent (D-141) directly. DECISIONS.md D-149 through D-155.

- [x] **Routing** (`app/(dashboard)/business-analysis/*`) — three new routes, plus one new `NAV_ITEMS` entry ("Business Analysis") applying F5's D-141 precedent directly — DECISIONS.md D-149
- [x] **`status.ts`** — `ANALYSIS_STATUS_PRESENTATION` (all 3 `AnalysisStatus` values → `StatusBadge` variant + label — `pending/completed/failed`, verified there is no separate "running" state), `ANALYSIS_STATUS_OPTIONS`, `isTerminalAnalysisStatus()`
- [x] **Data hooks** (`src/features/business-analysis/api/`) — `useBusinessAnalysis(leadId)` (`GET /leads/:id/business`, returns the single latest analysis or `null`; polls every 3s while `pending`, stops on `completed`/`failed`), `useTriggerBusinessAnalysis(leadId)` (`POST /leads/:id/business`, no request body — verified against the controller directly)
- [x] **Business Analysis Dashboard** (`business-analysis-dashboard-page.tsx`) — "Select Lead" (`LeadSelect`, reusing F4's real `useLeads()` hook directly rather than a second leads-fetching implementation — DECISIONS.md D-155), `leadId` lives in the URL (bookmarkable/reload-safe, same pattern as F2's `?period=`/F4's `?q=`); once selected, `BusinessAnalysisStatusPanel` shows current status, Run/Re-run, analysis version, provider/model, and an execution-status summary
- [x] **Run/Re-run Analysis** (`run-analysis-button.tsx`) — one mutation, one button whose label changes based on whether an analysis already exists — the backend never restricts re-running based on status (verified against `triggerAnalysis()` directly: it always either returns the cached completed row or creates a new version) — DECISIONS.md D-153
- [x] **Cache status** — shown as a one-time toast immediately after triggering ("Already up to date…" vs "Analysis started."), inferred from the mutation response's own `status` field (`completed` → cache hit, `pending` → cache miss) — never a persistent field, since the backend's real `cacheHit` signal is only ever an HTTP status code (200/202), discarded by `apiClient` before any caller could read it — DECISIONS.md D-151
- [x] **`BusinessAnalysisProgress`** — status-based, never simulated; two-step normal-path track (`pending → completed`, not three steps — there is no intermediate "running" state) plus a separate `failed` branch — DECISIONS.md D-154
- [x] **Analysis Details** (`business-analysis-details.tsx`) — every field `GET /leads/:id/business` actually returns: Business Summary, Industry, Target Audience, Brand Personality, Tone of Voice, Primary/Secondary Services, Unique Selling Points, Color Palette, Typography, Layout Style, Website Sections, SEO Keywords, Local SEO Suggestions, CTA Recommendations, Trust Signals, Social Proof Suggestions, Image Recommendations, Content Recommendations, AI Confidence Score, plus AI Provider, AI Model, Prompt Name, Prompt Version, Analysis Version, Token Usage, Estimated Cost, Execution Time, Created Date. **No Prompt Hash field** — exists on the Prisma model, never returned by the API — and **no persistent Cache Status field** — DECISIONS.md D-150, D-151
- [x] **Analysis History** (`business-analysis-history.tsx`) — exists as its own page per the explicit requirement, but shows exactly one entry (the latest analysis) with a visible sentence explaining why — the backend has no history/versions endpoint, `analysisVersion` increments but old rows are never exposed for reading — DECISIONS.md D-152
- [x] **Permissions** — `business:analyze` gates the Run/Re-run button only (hidden, not disabled, when missing); `leads:read` needs no gate (universal across all 6 roles) — no new permission introduced
- [x] **UI reuse** — DataTable (unused this module — no list to page, per D-152), StatusBadge, PermissionGate, ErrorState, Skeletons, AppShell all reused directly where applicable; zero duplicated components
- [x] Unit tests: 4 new (`status.test.ts`, `use-business-analysis.test.tsx` — fake-timers polling proof)
- [x] Integration tests: 27 new (`lead-select.test.tsx`, `run-analysis-button.test.tsx`, `business-analysis-status-panel.test.tsx`, `business-analysis-details.test.tsx`, `business-analysis-history.test.tsx`, `business-analysis-dashboard-page.test.tsx`) — 31 `src/features/business-analysis` tests total, 225 `apps/admin-web` tests total (up from 194)
- [x] Full validation: monorepo build/lint/typecheck/test clean; a real `next build` re-run clean — `/business-analysis` (2.69 kB), `/business-analysis/[leadId]` (5.34 kB), `/business-analysis/[leadId]/history` (1.4 kB), no RSC boundary issues
- [ ] **Not built, per explicit scope:** no backend API modified; no previous frontend module modified beyond the one additive `NAV_ITEMS` entry (and its one mechanically-updated test assertion), the same category of change F5 already established and got approval for
- [ ] **Known limitation, flagged not hidden:** "Prompt Hash" cannot be displayed — it exists in the database but is never returned by either endpoint (DECISIONS.md D-150)
- [ ] **Known limitation, flagged not hidden:** "Cache Status" is only ever knowable transiently, immediately after triggering — a later visit to Analysis Details cannot retroactively show whether the current row was originally a hit or a miss (DECISIONS.md D-151)
- [ ] **Known limitation, flagged not hidden:** Analysis History can never show more than one row — the backend genuinely has no way to retrieve superseded versions (DECISIONS.md D-152)
- [ ] **Not done in this environment:** no real Clerk application, backend, or AI provider was exercised — build/tests use dummy Clerk keys and a mocked `fetch` boundary, same constraint as F1-F5
- **Status: Module F6 implementation complete, pending your approval.**

## F7 — Theme Engine

Founder specified Theme Selection Dashboard/Theme Details/Theme Configuration up front, carrying forward F6's "poll while running, stop on terminal" instruction and hedging "Re-run Theme Selection (if backend supports)." Research against `apps/api/src/theme-engine/` found the real backend diverges more than the hedge anticipated: `ThemeConfiguration` has no status field at all (selection completes synchronously inside `POST`), several requested brand fields aren't part of the theme response at all, "Brand Style" isn't a real field anywhere, and there's no validation-results field. Every gap is resolved directly — by the backend's real behavior, by this module's own "never invent data" rule, or by applying F5/F6's twice-approved "no reserved nav slot" precedent a third time. DECISIONS.md D-156 through D-162.

- [x] **Routing** (`app/(dashboard)/theme-engine/*`) — three new routes, plus one new `NAV_ITEMS` entry applying F5/F6's D-141/D-149 precedent directly — DECISIONS.md D-156
- [x] **Data hooks** (`src/features/theme-engine/api/`) — `useThemeConfiguration(leadId)` (`GET /leads/:id/theme`, returns the single latest configuration or `null`; **no `refetchInterval`** — deliberately, since no status field exists to poll for), `useSelectTheme(leadId)` (`POST /leads/:id/theme`, no request body)
- [x] **No polling machinery anywhere in this module** — no `ThemeProgress` component, no status presentation map, no terminal-state predicate — `theme-selection.service.ts` awaits the AI recommendation step synchronously and always returns a complete, terminal row in the same response — DECISIONS.md D-157
- [x] **Theme Selection Dashboard** (`theme-dashboard-page.tsx`) — "Select Lead" (`LeadSelect`, a feature-local duplicate of F6's own component reusing F4's `useLeads()` directly, not a cross-feature import — DECISIONS.md D-162), `leadId` lives in the URL (bookmarkable/reload-safe); once selected, `ThemeStatusPanel` shows current theme status, Run/Re-run, compatibility score, selected theme, theme version, and engine version
- [x] **Precondition check** — the panel proactively verifies a completed business analysis exists (reusing F6's `useBusinessAnalysis()` hook directly) before offering "Run Theme Selection," since `POST /leads/:id/theme` throws `BusinessAnalysisNotFoundException` otherwise — links to Business Analysis instead of letting the user hit that error blind — DECISIONS.md D-161
- [x] **Run/Re-run Theme Selection** (`select-theme-button.tsx`) — one mutation, one button whose label changes; the backend never rejects a re-run (it either returns the cached configuration unchanged or creates a new version) — no cache-status toast built here, since the founder's F7 brief (unlike F6's) doesn't ask for one and the backend has no per-request fingerprint concept to infer one from anyway
- [x] **Theme Details** (`theme-details.tsx`) — Theme Name, Theme Version, Theme Hash, Compatibility Score, Selected At, Selected By Engine Version, plus Color Palette/Typography/Layout Style (real `ThemeConfiguration` fields); Brand Personality, Tone of Voice, Target Audience, Website Sections, and CTA Recommendations fetched separately from F6's `GET /leads/:id/business` (reused hook, not re-implemented) and clearly labeled "from AI Business Analyzer (M6), read-only" — DECISIONS.md D-158. **No "Brand Style" field** — not real anywhere — DECISIONS.md D-159
- [x] **Theme Configuration** (`theme-configuration-page.tsx`) — Theme Metadata, Selected Components (`componentSet`/`sectionOrder`), Theme Configuration Summary (navigation/hero/CTA/card/footer/animation/image styles, accessibility profile, mobile preferences), and "Other Themes Considered" (`rankedThemes`, honestly labeled — not "Compatibility Validation Results", since no such field exists — DECISIONS.md D-160), plus a conditional AI Recommendation section when the backend actually returned one. Read-only, no editing affordance anywhere — matches the founder's explicit instruction and the backend's own lack of any PATCH/PUT for this resource
- [x] **Permissions** — `theme:select` gates the Run/Re-run button only (hidden, not disabled, when missing); `leads:read` needs no gate (universal across all 6 roles) — no new permission introduced
- [x] **UI reuse** — DataTable (unused this module — no list to page here either), StatusBadge, PermissionGate, ErrorState, Skeletons, AppShell all reused where applicable; zero duplicated components
- [x] Unit tests: 8 new (`use-theme-configuration.test.tsx`, `select-theme-button.test.tsx`, `lead-select.test.tsx`)
- [x] Integration tests: 19 new (`theme-status-panel.test.tsx`, `theme-details.test.tsx`, `theme-configuration-page.test.tsx`, `theme-dashboard-page.test.tsx`) — 27 `src/features/theme-engine` tests total, 252 `apps/admin-web` tests total (up from 225)
- [x] Full validation: monorepo build/lint/typecheck/test clean; a real `next build` re-run clean — `/theme-engine` (2.49 kB), `/theme-engine/[leadId]` (4.67 kB), `/theme-engine/[leadId]/configuration` (1.94 kB), no RSC boundary issues
- [ ] **Not built, per explicit scope:** no backend API modified; no previous frontend module modified beyond the one additive `NAV_ITEMS` entry (and its one mechanically-updated test assertion), the same category of change F5/F6 already established and got approval for
- [ ] **Known limitation, flagged not hidden:** brand values on Theme Details come from the _latest_ business analysis, which may not exactly match the specific `businessAnalysisId` a given theme was actually selected from if analysis has been re-run since — no endpoint exists to fetch a `BusinessAnalysis` by id, so there is no alternative fetch to make (DECISIONS.md D-158)
- [ ] **Known limitation, flagged not hidden:** "Brand Style" cannot be displayed — not a real field anywhere in M6 or M7's schemas (DECISIONS.md D-159)
- [ ] **Not done in this environment:** no real Clerk application, backend, or AI provider was exercised — build/tests use dummy Clerk keys and a mocked `fetch` boundary, same constraint as F1-F6
- **Status: Module F7 implementation complete, pending your approval.**

## F8 — Website Generator

Founder specified Website Generator Dashboard/Layout Viewer/Component Viewer/Content Viewer/Generated Website Overview, scoping permissions to `leads:read`/`website:assemble` only. Research against `apps/api/src/{layout-engine,component-engine,content-engine,website-assembly}/` found a hard, unbreakable Layout→Component→Content→Website dependency chain with **no auto-orchestration anywhere** — `POST /leads/:id/website` throws immediately if Content doesn't already exist, and nothing else in the codebase can generate the earlier stages. The founder was asked directly and approved driving the full pipeline with all four real backend permissions (`layout:generate`/`component:generate`/`content:bind`/`website:assemble`) rather than one button scoped to `website:assemble` alone that would 404 on first use. DECISIONS.md D-163 through D-169.

- [x] **Routing** (`app/(dashboard)/website-generator/*`) — five new routes, plus one new `NAV_ITEMS` entry applying F5/F6/F7's precedent a fourth time — DECISIONS.md D-163
- [x] **Data hooks** (`src/features/website-generator/api/`) — one GET hook + one POST mutation hook per M8 resource (`use-layout-configuration`/`use-generate-layout`, `use-component-manifest`/`use-generate-components`, `use-content-manifest`/`use-bind-content`, `use-generated-website`/`use-assemble-website`) — **no `refetchInterval` anywhere**, since all four resources are synchronous and deterministic with no status field (verified against all four Prisma models/schemas and service files) — DECISIONS.md D-165
- [x] **Website Generator Dashboard** (`website-generator-dashboard-page.tsx` → `website-pipeline-panel.tsx`) — "Select Lead" (feature-local `LeadSelect` duplicate reusing F4's `useLeads()`), `leadId` in the URL; the panel first checks for a selected Theme (F7, linking to `/theme-engine?leadId=` if missing), then renders four `PipelineStageCard`s (Layout/Components/Content/Website) — each showing real generation status, its own real permission-gated action, and a real blocked-reason message when its own upstream prerequisite is missing — DECISIONS.md D-164
- [x] **Layout Viewer** (`layout-viewer.tsx`) — Layout Structure/Sections/Order (`pageStructure`, re-sorted by `order`), Hierarchy (`componentPlaceholders`), Layout Metadata (navigation/hero/footer/sidebar/grid/responsiveRules/ctaPlacements/layoutEngineVersion). Read-only — no input, no mutation call anywhere on the page
- [x] **Component Viewer** (`component-viewer.tsx`) — a `<DataTable/>` summary (per the explicit UI-reuse instruction) plus full detail cards per component: Component Name (`componentId` — no separate name field exists — DECISIONS.md D-166), Component Type, Component Configuration (theme token references, responsive rules, visibility), Component Properties (required/optional content slots, placeholders), Component Metadata (parent/children, accessibility). Read-only, no editable components rendered
- [x] **Content Viewer** (`content-viewer.tsx`) — Text Content and CTA Content shown together as one "Bound Content" `DataTable`, since the backend doesn't separately flag which bindings are CTA-specific (DECISIONS.md D-167); SEO Content (keywords/local SEO/meta title/meta description + structured data); Images as opaque `photoReference` tokens only, never rendered as `<img>`; Source Information as its own table column (every value's real `source` field); Content Metadata; Unresolved Bindings shown explicitly, never fabricated
- [x] **Generated Website Overview** (`generated-website-overview.tsx`) — Website Version, Generator Version, Created Date, Generated Files Summary (pure client-side `.length`/`.map(f => f.path)` over the real `files` array — never regeneration — DECISIONS.md D-168), Theme Used/Theme Configuration Version/Business Analysis Version (resolved via F6's/F7's reused GET hooks, since `GeneratedWebsite` carries only raw foreign-key ids — DECISIONS.md D-169)
- [x] **Permissions** — `layout:generate`/`component:generate`/`content:bind`/`website:assemble` each gate their own stage's action (hidden, not disabled, when missing); `leads:read` needs no gate (universal) — no permission beyond the real backend's own five decorators
- [x] **UI reuse** — DataTable (Component/Content viewers), StatusBadge (pipeline stage status), PermissionGate, ErrorState, Skeletons, AppShell all reused directly; zero duplicated components
- [x] Unit tests: 7 new (`use-layout-configuration.test.tsx`, `pipeline-stage-card.test.tsx`, `lead-select.test.tsx`)
- [x] Integration tests: 27 new (`website-pipeline-panel.test.tsx`, `website-generator-dashboard-page.test.tsx`, `layout-viewer.test.tsx`, `component-viewer.test.tsx`, `content-viewer.test.tsx`, `generated-website-overview.test.tsx`) — 34 `src/features/website-generator` tests total, 286 `apps/admin-web` tests total (up from 252)
- [x] Full validation: monorepo build/lint/typecheck/test clean; a real `next build` re-run clean — `/website-generator` (2.88 kB), `/website-generator/[leadId]` (4.37 kB), `/website-generator/[leadId]/layout` (2.01 kB), `/website-generator/[leadId]/components` (1.92 kB), `/website-generator/[leadId]/content` (2.33 kB), no RSC boundary issues
- [ ] **Not built, per explicit scope:** no backend API modified; no previous frontend module modified beyond the one additive `NAV_ITEMS` entry (and its one mechanically-updated test assertion); no regeneration of any kind on the frontend — every "Generate" action calls the real backend endpoint, the frontend never computes layout/component/content/website data itself
- [ ] **Known limitation, flagged not hidden:** "Component Name" is `componentId` — no separate display name exists anywhere in the backend (DECISIONS.md D-166)
- [ ] **Known limitation, flagged not hidden:** Text and CTA content cannot be split into separate sections — the backend has no field distinguishing them (DECISIONS.md D-167)
- [ ] **Not done in this environment:** no real Clerk application or backend was exercised — build/tests use dummy Clerk keys and a mocked `fetch` boundary, same constraint as F1-F7
- **Status: Module F8 implementation complete, pending your approval.**

## F9 — Website Preview

Founder's brief scoped permissions to `leads:read`/`website:preview`, matching the real backend exactly. Research against `apps/api/src/website-preview/` found no `POST` anywhere in M9 — all three `GET`s (`/leads/:id/preview`, `/preview/validation`, `/preview/readiness`) synchronously compute-and-cache their own artifact inline, and the GET itself is `website:preview`-gated as the privileged action. Several founder-requested fields (Publish Recommendation/Reasons on Publish Readiness, Deductions on Validation Report, live rendered pixels on Desktop/Tablet/Mobile Preview) don't exist on the backend and are resolved by omission — each case forced directly by the brief's own "never invent data"/"never regenerate"/"never calculate scores or readiness on the frontend" rules, not requiring founder escalation. DECISIONS.md D-170 through D-175.

- [x] **New shared primitives** (`packages/ui`) — `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` (new `@radix-ui/react-tabs` wrapper) and `Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardContent`/`CardFooter` — F9's first real need for either, per the founder's own "reuse existing components: ... Tabs ... Cards" instruction — DECISIONS.md D-170
- [x] **Routing** (`app/(dashboard)/website-preview/*`) — four route groups (Dashboard, `[leadId]/preview`, `[leadId]/validation`, `[leadId]/readiness`), plus one new `NAV_ITEMS` entry — unlike F5-F8, this entry is gated on `website:preview` itself (not `null`), since unlike those modules, viewing here is genuinely privileged for every route — DECISIONS.md D-171
- [x] **Data hooks** (`src/features/website-preview/api/`) — three GET-only hooks (`use-website-preview`, `use-validation-report`, `use-readiness-report`), no mutations at all (no POST exists on the backend), no polling (every GET is synchronous)
- [x] **Website Preview Dashboard** (`website-preview-dashboard-page.tsx` → `preview-status-panel.tsx`) — Select Lead (feature-local `LeadSelect` duplicate reusing F4's `useLeads()`), `leadId` in the URL; checks for an existing `GeneratedWebsite` (reuses F8's `useGeneratedWebsite()` directly) before ever calling the privileged preview endpoint, blocking with a link to Website Generator when missing; "Generate Preview" is omitted — opening the page is itself what generates/refreshes the preview — DECISIONS.md D-172
- [x] **Responsive Preview** (`responsive-preview.tsx`, one route: `[leadId]/preview`) — Desktop/Tablet/Mobile delivered as one `Tabs`-driven view (`?device=` URL-persisted), never a second fetch on switching; each device frame renders an honestly-labeled structural file manifest sized to the viewport, not a live rendered screenshot (the backend returns no HTML/screenshot for this resource) — DECISIONS.md D-173
- [x] **Validation Report** (`validation-report.tsx`) — flat `rules[]` grouped by `ruleCategory` into five `Tabs` (Structural/Content/SEO/Accessibility/Performance), bucketed by `status` into Passed/Failed/Warnings counts and lists, each showing its Validation Message and (if present) Recommendation; "Deductions" omitted — no such field exists on this resource — DECISIONS.md D-174
- [x] **Publish Readiness** (`publish-readiness.tsx`) — Overall/SEO/Accessibility/Performance/Content/Structure Score, each a `Card` showing score/maxScore and every real Deduction (ruleName, points, reason); "Publish Recommendation"/"Reasons" omitted — neither field exists, and deriving either would itself violate "never calculate readiness on the frontend" — DECISIONS.md D-175
- [x] **Permissions** — `website:preview` gates the entire content of every one of F9's four pages (not just a button) — the backend's own audited GETs are the privileged action here, a genuine deviation from F5-F8's "viewing is ungated, only the trigger is gated" shape
- [x] **UI reuse** — StatusBadge, PermissionGate, ErrorState, Skeleton, AppShell all reused directly; Tabs and Card newly promoted and reused across all four pages; zero duplicated components beyond the feature-local `LeadSelect`/`FieldRow`/`ListField` convention already established in F6-F8
- [x] Unit tests: 5 new (`use-website-preview.test.tsx`, `lead-select.test.tsx`)
- [x] Integration tests: 18 new (`preview-status-panel.test.tsx`, `website-preview-dashboard-page.test.tsx`, `responsive-preview.test.tsx`, `validation-report.test.tsx`, `publish-readiness.test.tsx`) plus 4 new `packages/ui` tests (`tabs.test.tsx`, `card.test.tsx`) — 23 `src/features/website-preview` tests total
- [x] Full validation: monorepo build/lint/typecheck/test clean; a real `next build` re-run clean
- [ ] **Not built, per explicit scope:** no backend API modified; no previous frontend module modified beyond the one additive (and, uniquely, permission-gated) `NAV_ITEMS` entry and its mechanically-updated test assertions; no regeneration of any kind — every "preview" is a read-only display of exactly what the backend already computed
- [ ] **Known limitation, flagged not hidden:** Desktop/Tablet/Mobile Preview shows a structural file manifest, not a live rendered screenshot — the backend returns no HTML/screenshot for this resource (DECISIONS.md D-173)
- [ ] **Known limitation, flagged not hidden:** "Publish Recommendation" and "Reasons" cannot be displayed — neither field exists on the backend, and computing them client-side is explicitly forbidden by the founder's own brief (DECISIONS.md D-175)
- [ ] **Not done in this environment:** no real Clerk application or backend was exercised — build/tests use dummy Clerk keys and a mocked `fetch` boundary, same constraint as F1-F8
- **Status: Module F9 implementation complete, pending your approval.**

## F10 — Sales CRM

Founder's brief listed `crm:view`/`crm:manage`/`crm:assign`/`crm:report` — an exact match for the real backend's four CRM permissions, no permission gap this time. Research against all five M10 sub-engines found one genuine architectural gap requiring founder escalation (Pipeline Board has no bulk "leads with their CRM stage" endpoint — resolved as a founder-approved bounded/paginated real board) plus several field-mapping gaps resolved directly. DECISIONS.md D-176 through D-181.

- [x] **New shared primitive** (`packages/ui`) — `Dialog`/`DialogTrigger`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogDescription`/`DialogFooter`/`DialogClose` (reuses the `@radix-ui/react-dialog` dependency already installed for `Sheet`) — F10's first real need for a centered modal, per the founder's own "reuse existing components: ... Dialogs" instruction — DECISIONS.md D-176
- [x] **Routing** (`app/(dashboard)/crm/*`) — replaces the F1-era placeholder at `/crm` with the real CRM Dashboard; adds `/crm/pipeline`, `/crm/leads/[leadId]`, `/crm/tasks`, `/crm/activities`, `/crm/proposals`, `/crm/reports`. No new `NAV_ITEMS` entry needed — the `crm` item already existed (RBAC alignment, gated `crm:view`)
- [x] **Pipeline Board** (`pipeline-board-page.tsx`) — founder-approved bounded/paginated real Kanban: leads accumulate via `useBoardLeads()` (infinite query over `GET /leads`, "Load More"), each cross-referenced with a real `GET /leads/:id/crm` (`useBoardLeadsCrm`, batched via `useQueries`, sharing a cache key with `useLeadCrm`). Real drag-and-drop AND an explicit stage-picker per card, both calling the one real `POST /leads/:id/crm/stage` endpoint — never fake. Moving into an `isLost` stage prompts for a lost reason first — DECISIONS.md D-177
- [x] **CRM Dashboard** (`crm-dashboard-page.tsx`) — reuses F2's `useCrmDashboard()` directly (same endpoint, same query key, no duplicate fetch). Pipeline Summary/Sales Performance/Conversion Rate/Win Rate/Lost Reasons/Average Sales Cycle are direct `DashboardStats` fields; "Assigned Leads" is `salesPerformanceByRep`'s per-rep counts (no dedicated field exists — DECISIONS.md D-179); "Upcoming Tasks" is a real separate `GET /crm/tasks?status=pending&dueBefore=` call. Gated `crm:report` (stricter than the page's own `crm:view` gate)
- [x] **Lead CRM Details** (`lead-crm-details-page.tsx`) — Lead Information (reused F4 `useLead`), CRM Status + inline stage-change (`crm:manage`), Assigned User + inline reassignment (`crm:assign`), Tasks, Activities (filtered to 4 real types), Proposals (read-only), Website Status (`GET /leads/:id/website-status`, `leads:read`-gated per M10's own Decision 8), Timeline (same activity feed, unfiltered — DECISIONS.md D-180)
- [x] **Tasks** (`tasks-page.tsx` + dialogs) — full List/Create/Edit/Complete/Cancel against `GET/PATCH /crm/tasks`, `GET/POST /leads/:id/tasks`. Complete and Cancel are both the same `PATCH .../tasks/:id` with `{status}` — no dedicated action route exists on the backend
- [x] **Activities** (`activities-page.tsx`) — Select Lead (no cross-lead activity endpoint exists), then Calls/Meetings/Emails/WhatsApp/Notes as five separate `Tabs` panels; Notes reuses F4's `useLeadNotes()`, never merged into the activity array — genuinely separate backend entities — DECISIONS.md D-180
- [x] **Proposals** (`proposals-page.tsx`) — Select Lead (no global proposal list exists), then read-only Version/Status/Sent/Viewed/Accepted/Rejected. Zero create/edit/status-change affordance anywhere, per the brief's own "Read-only tracking" framing — DECISIONS.md D-181
- [x] **Reports** (`reports-page.tsx`) — same `GET /crm/dashboard` endpoint as the Dashboard, differentiated by real `fromDate`/`toDate` filter controls and full breakdown tables rather than duplicated summary cards — DECISIONS.md D-178
- [x] **Permissions** — `crm:view` gates all seven pages' entire content (not just an action) via a shared `CrmViewGate`; `crm:manage` gates task/stage mutations; `crm:assign` gates owner reassignment; `crm:report` gates Dashboard/Reports specifically
- [x] **UI reuse** — DataTable (Tasks), StatusBadge, PermissionGate, Cards, Tabs, Dialogs (new), Forms, Skeletons, ErrorState, AppShell all reused; zero duplicated shared components beyond the established feature-local `LeadSelect` convention
- [x] Unit tests: 6 new (`use-lead-crm`, `use-transition-stage-any`, `lead-select`, `task-row-actions`, `create-task-dialog` × its own validation cases)
- [x] Integration tests: 21 new across `tasks-page`, `crm-dashboard-page`, `pipeline-board-page`, `lead-crm-details-page`, `activities-page`, `proposals-page`, `reports-page` — 27 `src/features/crm` tests total, plus 3 new `packages/ui` tests (`Dialog`)
- [x] Full validation: monorepo build/lint/typecheck/test clean; a real `next build` re-run clean
- [ ] **Not built, per explicit scope:** no backend API modified; no previous frontend module modified; no proposal content editor or AI-generation UI (backend has no endpoint for either); no drag-and-drop faked — every stage move calls the real transition endpoint
- [ ] **Known limitation, flagged not hidden:** Pipeline Board only reflects currently-loaded (paginated) leads, not the entire pipeline at a glance — a real backend gap (no bulk `LeadCRM` list endpoint), founder-approved resolution (DECISIONS.md D-177)
- [ ] **Known limitation, flagged not hidden:** Proposal status transitions (Send/Accept/Reject) are real, permitted backend actions this module deliberately doesn't expose, per the brief's own "read-only tracking" framing rather than a backend gap (DECISIONS.md D-181)
- [ ] **Not done in this environment:** no real Clerk application or backend was exercised — build/tests use dummy Clerk keys and a mocked `fetch` boundary, same constraint as F1-F9
- **Status: Module F10 implementation complete, pending your approval.**

## F11 — Deployment

Founder's brief listed `deployment:view`/`create`/`rollback`/`manage` — an exact match for the real backend's four Deployment permissions, no permission gap. Every M11 endpoint (Deployment/Health Check/Domain/Rollback Engines) is scoped under a lead, so this module follows F6-F9's "Select Lead" pattern rather than F10's cross-lead pages. Four brief-to-field mappings (Trigger Type, Rollback Availability, Deployment Logs, DNS Status) resolved directly against the real schema — DECISIONS.md D-187/D-188.

- [x] **Routing** (`app/(dashboard)/deployment/*`) — new Deployment Dashboard at `/deployment`, plus `/deployment/[leadId]/history`, `/deployment/[leadId]/domains`, `/deployment/[leadId]/deployments/[deploymentId]`, `/deployment/[leadId]/deployments/[deploymentId]/health`. One additive `NAV_ITEMS` entry gated on `deployment:view` explicitly (not `null`) — DECISIONS.md D-187
- [x] **Deployment Dashboard** (`deployment-status-panel.tsx`) — `GET /leads/:id/deployment-status` rollup (Current Deployment/Status/Provider/Environment/Version/Deployment Time/Latest Health Status/Production Ready) plus a real "Deploy" action (`POST /leads/:id/deployments`, gated `deployment:create`), never simulated — real backend errors (`GENERATED_WEBSITE_NOT_FOUND`/`DEPLOYMENT_VALIDATION_FAILED`) surface via toast, verified live against the real API
- [x] **Deployment History** (`deployment-history-page.tsx`) — `GET /leads/:id/deployments`, cursor-paginated, newest-version-first; derived "Trigger Type" column (Manual/Retry/Rollback) computed from `retryOfDeploymentId`/`rollbackFromDeploymentId` — DECISIONS.md D-188
- [x] **Deployment Details** (`deployment-details-page.tsx`) — every `WebsiteDeployment` field across five cards (Deployment/Build/Commit/Logs/Metadata) plus derived Rollback Availability; real Retry (`deployment:create`, FAILED-only) and Rollback (`deployment:rollback`, COMPLETED+HEALTHY-only) actions
- [x] **Domain Management** (`domain-management-page.tsx` + `register-domain-dialog.tsx`) — `GET`/`POST /leads/:id/domains`, `POST .../verify`, both gated `deployment:manage`; not read-only, since the backend isn't. Verified end-to-end against the real API (register → real Pending/Pending row → verify → real "Verification requested")
- [x] **Health Monitoring** (`health-monitoring-page.tsx`) — `GET`/`POST /leads/:id/deployments/:deploymentId/health[-check]`; summary stats (Health Status/Response Time/Last Check/Passed/Failed Checks) read off the single latest check row only, never a computed score; paginated check history table; real "Run Health Check" trigger gated `deployment:manage`
- [x] **Permissions** — `deployment:view` gates the nav entry; `deployment:create` gates Deploy/Retry; `deployment:rollback` gates Roll Back; `deployment:manage` gates domain registration/verification and manual health-check triggers — all via `PermissionGate`, hidden not disabled
- [x] **UI reuse** — DataTable, StatusBadge, PermissionGate, Cards, Dialog, Skeletons, ErrorState all reused; zero duplicated shared components beyond the established feature-local `LeadSelect`/`detail-primitives` convention
- [x] Unit tests: 17 new (`format` 5, `trigger-type` 4, `rollback-eligibility` 4, `health-check-detail` 4), plus 2 new `auth.test.ts` nav-visibility assertions
- [x] Integration tests: 32 new across `deployment-dashboard-page`, `deployment-status-panel`, `deployment-history-page`, `deployment-details-page`, `domain-management-page`, `health-monitoring-page` — 51 new tests total (`apps/admin-web` up from 338 to 389)
- [x] Full validation: monorepo build/lint/typecheck/test clean (103 test files, 389 tests); a real `next build` re-run clean; verified live in a real browser against the real API — lead search, Deploy (real validation error), Domain register/verify (real success) all confirmed working end-to-end
- [ ] **Not built, per explicit scope:** no backend API modified; no previous frontend module modified beyond the one additive `NAV_ITEMS` entry; no DNS automation; no client-side health score or readiness calculation
- [ ] **Not done in this environment:** exercised against this session's local dev-auth-bypassed backend (real Postgres/Redis, real seeded data), not a real Clerk application in production mode
- **Status: Module F11 implementation complete, pending your approval.**

## F12 — Analytics & Reporting

Founder's brief listed `analytics:view`/`report`/`export`/`manage` — an exact match for the real backend's four Analytics permissions, no permission gap. M12's real HTTP surface is exactly three endpoints (dashboard/reports/export) composing eight dashboard widgets and fifteen report types — smaller than the brief's eight-page, twenty-metric description, reconciled directly against the real schemas. DECISIONS.md D-189 through D-193.

- [x] **Routing** (`app/(dashboard)/analytics/*`) — replaces the F1-era placeholder at `/analytics` with the real Analytics Dashboard; adds `/analytics/reports`, `/analytics/business`, `/analytics/usage`, `/analytics/system`, `/analytics/costs`, `/analytics/audit`, `/analytics/activity`. No new `NAV_ITEMS` entry needed — `analytics` already existed (gated `analytics:view` since RBAC Alignment)
- [x] **Analytics Dashboard** (`analytics-dashboard-page.tsx`) — reuses F2's `useAnalyticsDashboard()` directly (same endpoint, same query key); real eight widgets grouped under Business Analytics/Usage Analytics/System Monitoring headings; links to Audit Logs/User Activity (no dashboard widget exists for either); own feature-local period+range control (Custom Range support F2's `PeriodSelect` lacks) — DECISIONS.md D-189
- [x] **Analytics Reports** (`analytics-reports-page.tsx`) — selector covering all fifteen real `REPORT_TYPES`, one real renderer per type (`reports/report-view.tsx` dispatcher, reused by every themed page's deep links); real Daily/Weekly/Monthly/Yearly/Custom Range filters; real "Export CSV" button (only implemented format)
- [x] **Business/Usage/System/Cost Analytics** (`business-analytics-page.tsx`, `usage-analytics-page.tsx`, `system-monitoring-page.tsx`, `cost-analytics-page.tsx`) — lean widget-summary + `ReportLinkList` deep-link pages, not parallel report re-implementations — DECISIONS.md D-190
- [x] **Audit Logs** (`audit-logs-page.tsx`) — real cursor-paginated `audit` report in a real `DataTable`, entirely gated `analytics:report`
- [x] **User Activity** (`user-activity-page.tsx`) — real `user_activity` report, entirely gated `analytics:report`
- [x] **Permissions** — `analytics:view` gates every page's entry; `analytics:report` additionally gates all report-content sections (Reports/Audit Logs/User Activity in full, plus themed pages' report links); `analytics:export` gates the Export CSV button; `analytics:manage` has no corresponding action anywhere (backend enforces it on nothing yet)
- [x] **UI reuse** — DataTable, StatusBadge, PermissionGate, Cards, Skeletons, ErrorState all reused; no new chart library — reuses the existing hand-rolled `BarList` proportional-bar strategy (F2's `CrmPipelineSection` precedent)
- [x] Unit tests: 26 new (`format` 9, `use-analytics-period` 9, `report-labels` 4, `bar-list` 3, plus `analytics-view-gate`/`analytics-report-gate` unit-style permission tests)
- [x] Integration tests: 50 new across all 8 pages, `report-view` dispatcher (15 report types), `analytics-sub-nav` (navigation), `period-range-select`, `export-csv-button` — 76 new tests total (`apps/admin-web` up from 389 to 465)
- [x] Full validation: monorepo build/lint/typecheck/test clean; a real `next build` re-run clean, all 8 F12 routes present; verified live in a real browser against the real API — 12 of 15 report types confirmed rendering real data (Sales Funnel, AI Usage, Audit Logs, User Activity all screenshotted with real backend data), Export CSV verified end-to-end with real CSV content returned
- [ ] **Not built, per explicit scope:** no backend API modified; no previous frontend module modified beyond zero (no nav change needed); no chart library added; six brief-named concepts with no backend data source (API Usage, Preview Usage, Business Growth, plus three dashboard-widget-shaped items already covered by their own real reports) — DECISIONS.md D-191
- [ ] **Known limitation, environment-specific, flagged not hidden:** this session's local backend has no Upstash-REST-compatible Redis proxy (pre-existing gap, documented before F12 began), so `ai_cost`/`health`/`executive_dashboard`/the composed dashboard return real 500s locally — `ErrorState`+Retry confirmed correct via the test suite and a temporary debug pass; a compounding browser-automation-tool-specific TanStack Query online-detection quirk was also diagnosed and is not a real-user-browser behavior — DECISIONS.md D-193
- [ ] **Not done in this environment:** exercised against this session's local dev-auth-bypassed backend (real Postgres/Redis, real seeded data), not a real Clerk application in production mode
- **Status: Module F12 implementation complete, pending your approval.**

## F13 — Settings

Founder's live brief (this conversation): "the final implementation module before Production," eleven named pages (Settings Dashboard, Company, AI, API Keys, Prompts, Theme Defaults, Deployment, Cost & Budget, Analytics, System Information, Audit History), an explicit "Research First" mandate ("identify every real configurable setting... if something cannot be configured through existing backend APIs, display it as read-only"), and the same standing global rules every prior frontend module has followed (frontend only, never touch backend/database/APIs, reuse existing components/hooks, verify before changing a previous module). Named "F13" in the live brief, though F1's own roadmap freeze originally reserved that numeral for User Management (Settings was F14) — the live instruction was followed literally rather than the earlier numbering, since Team/User Management and Profile were never named in this brief and remain untouched backlog items — DECISIONS.md D-194.

- [x] **Backend research pass** (before any code) across every module's controllers/schema/shared-types: found real, live configuration surface for exactly one page (Company Settings, via M10's `CrmSettings` singleton, `GET`/`PATCH /crm/settings`); real _reporting_ data (not configuration) reusable from M12 for five pages (AI, Theme Defaults, Deployment, Cost & Budget, Audit History); zero backend surface at all for three pages (API Keys, Prompt Management's global view, Analytics Settings) — DECISIONS.md D-195 through D-205
- [x] Shared `NotConfigurable`/`NotConfigurableField` primitive (`src/features/settings/components/not-configurable.tsx`) — the one place every real "backend doesn't support this" gap is stated, never a disabled form control, never a guessed current value — DECISIONS.md D-195
- [x] **Settings Dashboard** (`settings-dashboard-page.tsx`) — a plain navigation hub (same shape as F12's own dashboard "More" links) plus one real quick-glance stat (Currency/Timezone from `CrmSettings`, gated `crm:view`)
- [x] **Company Settings** (`company-settings-page.tsx` + `company-settings-form.tsx`) — edits the three real `CrmSettings` scalar fields (`currency`/`timezone`/`defaultReminderMinutesBeforeDue`) via `PATCH /crm/settings`, gated `crm:manage` to edit / `crm:view` to read; validated against the exact shared `updateCrmSettingsSchema`; `businessHours` shown read-only (no weekly-schedule editor built, out of brief's scope); Company Name/Logo/Email/Phone/Website/Address `NotConfigurable` — no `Organization`/`Company` model exists anywhere in the schema — DECISIONS.md D-196
- [x] **AI Settings** (`ai-settings-page.tsx`) — real `GET /analytics/reports/ai_usage` (gated `analytics:report`) rendered via F12's existing `ReportView` dispatcher verbatim; AI Budget linked to Cost & Budget rather than re-fetched; Provider/Model/Temperature/Max Tokens/AI Limits `NotConfigurable` (no GET endpoint exposes them — DI-time constants only) — DECISIONS.md D-197
- [x] **API Keys** (`api-keys-page.tsx`) — fully informative; no key-management model/endpoint anywhere in the backend — DECISIONS.md D-198
- [x] **Prompt Management** (`prompt-management-page.tsx`) — states M6's real per-lead prompt-versioning architecture, links to the existing Business Analysis screen (F6) rather than fabricating a global cross-lead registry endpoint that doesn't exist — DECISIONS.md D-199
- [x] **Theme Defaults** (`theme-defaults-page.tsx`) — real `GET /analytics/reports/theme_usage` via `ReportView`; no "default theme" setting exists (M7 always ranks fresh), links to Theme Engine (F7) — DECISIONS.md D-200
- [x] **Deployment Settings** (`deployment-settings-page.tsx`) — real `GET /analytics/reports/deployment` via `ReportView`; Provider/Default Project/Production Domain/Preview Domain/Team `NotConfigurable` (environment variables, no GET endpoint), links to Deployment dashboard (F11) — DECISIONS.md D-201
- [x] **Cost & Budget** (`cost-budget-page.tsx`) — real `GET /analytics/reports/ai_cost` via `ReportView`/`AiCostReportView`; local Daily/Weekly/Monthly/Yearly toggle (Custom Range deliberately omitted — avoids reproducing a real, newly-found F12 bug where selecting Custom Range fires the report query before dates are picked, without modifying F12's shared hook); client-derived ≥80%-of-ceiling warning banner from the two real numbers the report returns — DECISIONS.md D-202
- [x] **Analytics Settings** (`analytics-settings-page.tsx`) — states real, code-verified fixed system behavior (CSV-only export, 1000-row cap) as facts, not settings; `NotConfigurable` for the (nonexistent) retention/threshold config surface — DECISIONS.md D-203
- [x] **System Information** (`system-information-page.tsx` + `use-health.ts`) — real, live `GET /health` polled every 30s (Reachable/Unreachable, real server timestamp); Application Version/Build Version/Git Commit/Environment/Database Status/Cache Status/Backend Version all `NotConfigurable` — no endpoint exposes any of them — DECISIONS.md D-204
- [x] **Audit History** (`audit-history-page.tsx`) — renders F12's own `AuditLogsPage` component verbatim inside F13's Settings shell; zero duplicated table/pagination/gating logic, per the brief's explicit "never build a second audit system" — DECISIONS.md D-205
- [x] **Access control** — every page wrapped in `SettingsAccessGate` (`team:manage`, matching the pre-existing `/settings` `NAV_ITEMS` entry from F1/RBAC Alignment — unchanged, no nav modification needed); narrower real permissions (`crm:view`/`crm:manage`, `analytics:report`) applied per-section beneath that outer gate, matching each page's real underlying endpoint's own requirement
- [x] **Routing** — `app/(dashboard)/settings/{page.tsx (replaced),company,ai,api-keys,prompts,theme-defaults,deployment,cost-budget,analytics,system,audit}/page.tsx`; only the placeholder body of the existing `/settings` route was replaced, same category of change every prior module's route-wiring step has made
- [x] Unit/integration tests: 31 new (`settings-sub-nav` nav/active-link, `settings-dashboard-page`, `company-settings-page` incl. `crm:manage`-gated form vs `team:manage` outer block, `ai-settings-page`, `api-keys-page`, `prompt-management-page`, `theme-defaults-page`, `deployment-settings-page`, `cost-budget-page` incl. period toggle + warning-banner threshold, `analytics-settings-page`, `system-information-page` incl. live Reachable/Unreachable, `audit-history-page` proving real reuse of F12's component) across 12 test files — `apps/admin-web` 496/496 total (up from 465)
- [x] Full validation: monorepo `tsc --noEmit`/lint/test clean; a real `next build` re-run clean, all 11 F13 routes present, no RSC boundary issue
- [ ] **Not built, per explicit scope:** no backend API modified; no previous frontend module modified beyond zero (the existing `/settings` `NAV_ITEMS` entry and F12's `AuditLogsPage` are both reused unmodified); no fabricated "current value" for any setting with no GET endpoint
- [ ] **Not done in this environment:** live-browser verification against a running dev server — this module's validation is the full mocked-`fetch` test suite plus a real production build, not an in-browser click-through session (unlike F1-F12, which each had one); exercised conceptually against this session's local dev-auth-bypassed backend assumptions only
- **Status: Module F13 implementation complete, pending your approval.**

## Backlog — not currently in the numbered roadmap

Carried forward from the old plan's M10/M11, not silently dropped — see docs/21-implementation-roadmap.md §5 and DECISIONS.md D-022 for context and next-step options.

- [ ] **Team & Settings** — `/team` endpoints (invite, role change), `/settings/profile` endpoint, Team & Settings UI. Not started.
- [ ] **Observability & Hardening** — full role-authorization test pass, cross-module cost-governance load test, alerting verification, OWASP security review, internal launch checklist sign-off. Not started. This was a non-negotiable pre-launch gate in the old plan — needs an explicit home before internal launch, not just a backlog entry.
