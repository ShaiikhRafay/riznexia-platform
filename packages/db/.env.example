# Needed when running Prisma CLI commands directly from this package
# (generate/migrate/studio/seed). apps/api reads its own copy at runtime —
# see apps/api/.env.example. Copy to .env and fill in.
DATABASE_URL="postgresql://riznexia:riznexia_dev@localhost:5432/riznexia_dev"
