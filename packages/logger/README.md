# @riznexia/logger

Structured JSON logging wrapper (Pino) with correlation-ID propagation, so a failure is traceable end-to-end across the async pipeline — see [System Architecture §14](../../docs/16-system-architecture.md#14-logging-new-relative-to-phase-1-docs). Currently empty scaffolding.
