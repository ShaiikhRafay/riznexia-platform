// Placeholder — shared components (data table, status badge, pipeline stepper,
// kanban board, drawer/panel primitives, per Doc 17 §6) land with their first
// consuming screen module (Doc 21).
export {};
