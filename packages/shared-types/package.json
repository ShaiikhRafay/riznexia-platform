{
  "name": "@riznexia/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and zod schemas — source of truth for web/api contracts (Doc 05, Doc 19).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@riznexia/config-eslint": "workspace:*",
    "@riznexia/config-typescript": "workspace:*",
    "eslint": "^9.11.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
