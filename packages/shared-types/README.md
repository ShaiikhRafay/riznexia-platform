# @riznexia/shared-types

Single source of truth for types shared between `apps/web` and `apps/api` — DTOs, enums, and zod validation schemas (Doc 05, Doc 19 §1). Currently empty scaffolding; populated alongside each feature module per [Doc 21](../../docs/21-implementation-roadmap.md).
