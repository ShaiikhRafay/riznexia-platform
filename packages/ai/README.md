# @riznexia/ai

The `AiService` client: provider adapters (Claude, image-gen), prompt template management, response parsing/validation — see [AI Agent Architecture](../../docs/20-ai-agents-architecture.md) and [System Architecture §7](../../docs/16-system-architecture.md#7-ai-layer). Currently empty scaffolding; the gateway and the first agents land in Module M6 — AI Business Analyzer (Business Analyzer Agent) and Module M7 — Theme Engine (Theme Selector Agent) (Doc 21).
