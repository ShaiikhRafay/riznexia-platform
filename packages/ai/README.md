# @riznexia/ai

The `AiService` client: provider adapters (Claude, image-gen), prompt template management, response parsing/validation — see [AI Agent Architecture](../../docs/20-ai-agents-architecture.md) and [System Architecture §7](../../docs/16-system-architecture.md#7-ai-layer). Currently empty scaffolding; the gateway and the first agents (Business Analyzer, Theme Selector) land in Module M3/M4 (Doc 21).
