{
  "name": "@riznexia/ai",
  "version": "0.1.0",
  "private": true,
  "description": "AiService: provider adapters, prompt registry, response validation — Doc 16 §7, Doc 20.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@riznexia/shared-types": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@riznexia/config-eslint": "workspace:*",
    "@riznexia/config-typescript": "workspace:*",
    "@types/node": "^22.0.0",
    "eslint": "^9.11.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
