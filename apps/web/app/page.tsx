// Placeholder root page — replaced by the real Dashboard Home (Doc 17 §9)
// once Module M2 (Lead Pipeline / CRM) and its dependencies land.
export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-text-secondary">Riznexia — scaffolding in progress.</p>
    </main>
  );
}
