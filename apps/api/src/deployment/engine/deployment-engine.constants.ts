// Module M11 — same apps/api-local module-version convention as
// theme-engine/theme-engine.constants.ts (THEME_ENGINE_VERSION). Stored
// on every WebsiteDeployment row (founder's explicit "Additional
// Requirements" field list).
export const DEPLOYMENT_ENGINE_VERSION = 'v1.0';
